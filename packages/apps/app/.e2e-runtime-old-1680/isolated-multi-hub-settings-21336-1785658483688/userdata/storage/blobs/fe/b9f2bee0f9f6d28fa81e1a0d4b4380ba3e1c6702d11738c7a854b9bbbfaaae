flowchart TD
    subgraph S1["1. Initial Client Setup & Information"]
        A1["Find Billing Account ID<br/>(Cost Management + Billing → Properties)"] --> A3
        A2["Find Tenant ID<br/>(Microsoft Entra ID → Overview)"] --> A3
        A3["Send both to your Normain contact"] --> A4["Normain creates the private offer"]
    end

    subgraph S2["2. Identifying the Right People"]
        B1{"Suitable Azure subscription?<br/>(Active + valid payment method)"}
        B1 -- "No" --> B2["Create one<br/>EA: Enterprise Admin / Account Owner<br/>MCA: Billing Owner / Contributor<br/>PAYG: account holder"]
        B1 -- "Yes" --> B3
        B2 --> B3["Identify who can:<br/>• Accept offer — billing admin<br/>• Purchase — subscription Owner / Contributor<br/>• Approve app access — Global Administrator"]
    end

    subgraph S3["3. Accepting the Private Offer"]
        C1["Step 1: Accept private offer<br/>(link → Azure Marketplace)"] --> C2["Wait 15–60 min<br/>for Purchase to activate"]
        C2 --> C3["Step 2: Purchase<br/>select subscription + resource group,<br/>name it, auto-renew off,<br/>Review + subscribe → Subscribe"]
        C3 --> C4["Step 3: Activate subscription<br/>on Normain website<br/>(purchase incomplete until done)"]
        C4 --> C5["Step 4: Sign in<br/>your-instance.normain.com<br/>with Microsoft work email"]
        C5 --> C6{"App approval needed?"}
        C6 -- "Self-approve" --> C7["Click Accept"]
        C6 -- "IT approval" --> C8["Request approval →<br/>IT approves via Enterprise applications →<br/>Admin consent requests"]
        C7 --> C9["Done — signed in"]
        C8 --> C9
    end

    A4 --> B1
    B3 --> C1
