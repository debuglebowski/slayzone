flowchart TB
  subgraph current["WHAT WE HAVE — Standard Marketplace SSO"]
    direction TB
    cA["Normain app registration<br/>(multi-tenant, in Normain Entra)"]
    cB["KPMG buys offer in Marketplace<br/>→ Global admin sees consent prompt"]
    cC["Consent would grant Normain access<br/>to KPMG GLOBAL Entra tenant"]
    cD["Enterprise app would auto-provision<br/>in KPMG Global tenant"]
    cE["Users sign in via OAuth<br/>(delegated user.read on signing-in user)"]
    cA --> cB --> cC --> cD --> cE
    block["BLOCKED<br/>KPMG Global policy: no 3rd-party apps<br/>permitted in the global directory<br/>(~250k users / 120+ member firms)"]
    cC -.-> block
    style block fill:#fdd,stroke:#c33,color:#000
  end

  subgraph target["WHAT THEY WANT — KPMG-owned SPN"]
    direction TB
    tA["KPMG provisions enterprise app / SPN<br/>directly inside KPMG Global Entra"]
    tB["NITSO security review + approval"]
    tC["SPN scoped: only KPMG Sweden users<br/>assigned to the enterprise app"]
    tD["KPMG hands app / principal ID<br/>to Normain"]
    tE["Normain SaaS configured to federate<br/>against this specific SPN"]
    tF["KPMG users sign in<br/>via KPMG-owned SPN<br/>Normain receives identity token"]
    tA --> tB --> tC --> tD --> tE --> tF
    expand["Future expansion (DE / FI / etc.):<br/>KPMG adds users to the SPN<br/>→ zero change on Normain side"]
    tF -.-> expand
    style expand fill:#dfd,stroke:#393,color:#000
  end

  current ~~~ target
