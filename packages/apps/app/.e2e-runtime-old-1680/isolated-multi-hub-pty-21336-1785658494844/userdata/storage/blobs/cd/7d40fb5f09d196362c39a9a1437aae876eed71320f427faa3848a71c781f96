# Phase 0 — Remaining failing tests (post round 12)

## ✅ Round 12 fixed
- `33-pty-respawn-forced` ALL 4 tests — fetches were happening from renderer (blocked by CSP `connect-src` on `http://127.0.0.1:*`); moved to Playwright test context
- `14-project-settings:454` — testid rename `columns` → `tasks/statuses`
- `22-terminal-mode-switching:140` — Radix Select races on temp tasks; bypass via direct DB update

## Still failing — sorted by effort

### 🔴 Large (5)
- `93-artifacts-panel` ×7 — test-state-dependent ordering issues (rename → delete → empty folder etc.); each needs individual investigation
- `42-terminal-tab-isolation:26` Cmd+K
- `24-browser-panel:145` Alt+Shift+Click
- `61-web-panel-handoff-routing:182`
- `71-webview-popup-handling:540/696`
- `32-git-diff-panel:171/219`

### 💀 Very Large (3)
- `90-terminal-fd-limit:34` RLIMIT
- `72-browser-view-lifecycle:184` Bug 1
- `77-browser-view-focus:89` glow

## Tally
🟢 0 · 🟡 0 · 🔴 6 · 💀 3 → **9 rows / ~14 tests** still failing

Cumulative: **80 → ~14 (~83% reduction)**.
