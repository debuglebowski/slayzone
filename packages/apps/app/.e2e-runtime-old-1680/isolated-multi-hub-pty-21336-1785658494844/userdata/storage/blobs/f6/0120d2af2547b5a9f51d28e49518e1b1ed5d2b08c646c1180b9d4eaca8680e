# Phase 2 — tRPC transport layer (rip-per-handler)

> **Master plan**: `server-mode-implementation-plan.md` on parent task `1f788452-581e-429f-9427-a1d61e8a349b` (asset id `1eecca69-7d62-4763-8300-a5e9e4fcf894`).
>
> **Read first** (in master): §1 Context, §2 Decisions, §3 Audit Findings, §7 Conventions, §9 Boundaries, §11 Deferred. Then this phase.
>
> Master is canonical for cross-cutting decisions; this asset is the tactical home for this phase only.

---

## Phase scope

Build tRPC router. Replace IPC handlers in-place with tRPC procedures. Delete IPC code as you go — no compat layer.

**Tasks:**
- [ ] Add tRPC dependencies. Server: `@trpc/server`. Client: `@trpc/client`, `@trpc/react-query`. WebSocket: `ws` + tRPC's WS adapter.
- [ ] Create `packages/shared/transport/` (or `packages/shared/trpc/`) with: root `appRouter`, per-domain sub-routers, context type, WS server adapter, type-only client export.
- [ ] WebSocket server inside Electron main (Phase 4 will move it to standalone `@slayzone/server` pkg too).
- [ ] **Per IPC handler**: write tRPC procedure (query/mutation/subscription) → migrate renderer call site → delete IPC handler in same PR. One handler at a time, no big-bang.
- [ ] Streaming events (PTY data, agent chat deltas, `tasks:changed`) → tRPC subscriptions over WS.
- [ ] Asset upload procedure: enforce **100 MB per-file limit**, configurable via env (`SLAYZONE_MAX_UPLOAD_BYTES`).
- [ ] tRPC procedures are type-imported by renderer — no zod registry needed; tRPC + TS infer end-to-end.

**Done criteria:** `ipcMain.handle` and `webContents.send` are gone (or at least IPC channel count is decreasing PR-by-PR until zero). Renderer talks tRPC over WS to localhost.

---

## Cross-phase dependencies

- **Upstream**: Phase 1 (domain code must be Electron-free so tRPC procedures wrap pure functions).
- **Downstream**: Phase 3 (`@slayzone/server` package serves the same tRPC router from a standalone process). Phase 4 (renderer connects via tRPC client).

---

## Phase-specific notes

(Add findings, decisions, deviations, links, sub-PR references here as work progresses.)

### Per-handler migration log

(Track each IPC handler → tRPC procedure conversion here. One sub-PR per handler. Format: `handler-name | sub-PR | renderer call site | status`.)

---

## Verification checklist

- [ ] Done criteria met (`ipcMain.handle` count = 0, `webContents.send` count = 0)
- [ ] Tests added per §5 of master ("tRPC contract", "WS protocol", "Per-handler migration" rows)
- [ ] No regression in existing E2E suite
- [ ] Phase 1 deliverables consumed (procedures call into framework-free domain code)
- [ ] tRPC types imported by renderer compile cleanly end-to-end
