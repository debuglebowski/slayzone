flowchart TD
    LENS["Editorial lens: cut through the noise<br/>money + fear draw chaotic voices — aim for clarity over confusion"]
    MAP["THE MAP — series overview<br/>(one brief pass over every theme below)"]

    LENS --> MAP

    subgraph T1["1 · What AI actually is"]
        direction TB
        T1a["'AI' is just the everyday word for today's LLMs — no jargon needed"]
        T1b["You judge it by what you observe when using it, not by theory"]
        T1c["'It's just autocomplete' is a comforting rationalization — assume it keeps improving"]
        T1d["It keeps getting smarter at concrete things: execution (code, summaries, reports), reasoning, orchestration"]
        T1e["Emergent capabilities: complex behavior emerges from simple primitives"]
        T1f["Open vs. closed models"]
        T1a --> T1b --> T1c --> T1d --> T1e --> T1f
    end

    subgraph T2["2 · Where it already touches your life"]
        direction TB
        T2a["AI you already use without realizing it"]
        T2b["Generative tools: real uses, real limits"]
        T2c["AI at work: automation vs. augmentation"]
        T2d["The breadth: use cases across the whole spectrum"]
        T2a --> T2b --> T2c --> T2d
    end

    subgraph T3["3 · The risks"]
        direction TB
        T3a["Privacy: what happens to what you type"]
        T3b["AI-enabled scams and fraud"]
        T3c["Deepfakes and misinformation"]
        T3d["Bias in automated decisions"]
        T3a --> T3b --> T3c --> T3d
    end

    subgraph T4["4 · What stays human — and what you'll need"]
        direction TB
        T4a["Human vs. AI capability trajectory — where each is ahead, and the curves"]
        T4b["Wave 1: replacing solo tasks -> Wave 2: orchestrating agent swarms (the collaboration frontier)"]
        T4c["Any 'what's still needed?' answer needs a time horizon — meaningful to ~24mo, guesswork beyond"]
        T4d["Prompt engineering is dying — being explicit matters less; taste & judgment on what's correct matters more"]
        T4e["Core human traits to lean into: connection, clarity, being *more* technically enabled"]
        T4a --> T4b --> T4c --> T4d --> T4e
    end

    subgraph T5["5 · The big picture — society & power"]
        direction TB
        T5a["Jobs and the economy: hype vs. realistic"]
        T5b["Who controls this, and why it matters"]
        T5c["EU AI Act: what it actually means for you"]
        T5d["The AGI / superintelligence debate"]
        T5a --> T5b --> T5c --> T5d
    end

    MAP --> T1
    MAP --> T2
    MAP --> T3
    MAP --> T4
    MAP --> T5

    G["Glossary teaser: model, product, MCP, skills, API, CLI<br/>(separate on-demand video)"]
    MAP -.-> G
