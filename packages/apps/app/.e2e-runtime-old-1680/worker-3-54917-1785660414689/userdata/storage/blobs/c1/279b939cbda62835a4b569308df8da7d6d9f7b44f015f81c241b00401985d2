flowchart TB
    subgraph noun["THE SHARED NOUN — 'View' (saved query over a concept)"]
        V["{ conceptId, conditions, match,<br/>columns, sort, displayMode }"]
    end

    subgraph renderers["RENDERERS — same View, different chrome"]
        R1["ConceptView page<br/>full table · quick-edit ·<br/>URL params = ephemeral overlay"]
        R2["Dashboard list widget<br/>compact · read-only today"]
        R3["Sidebar list<br/>mini (already same conditions)"]
        R4["future: named saved views<br/>per concept (tabs on page)"]
    end

    V --> R1
    V --> R2
    V --> R3
    V --> R4

    subgraph differs["WHAT STAYS DIFFERENT (deliberately)"]
        D1["cardinality: 1 canonical auto page<br/>per concept vs N owned dashboards"]
        D2["ownership: concept page org-shared,<br/>dashboards personal"]
        D3["chrome: editing table<br/>vs monitoring widget"]
    end

    subgraph endstate["END-STATE FORK"]
        E1["A: table + pinned widget strip<br/>(metrics/breakdown, conceptId pre-bound)<br/>— table keeps viewport, low config burden"]
        E2["B: concept page IS a canvas<br/>(table = one big widget)<br/>— max flexibility, every page needs config"]
    end

    renderers --> endstate
