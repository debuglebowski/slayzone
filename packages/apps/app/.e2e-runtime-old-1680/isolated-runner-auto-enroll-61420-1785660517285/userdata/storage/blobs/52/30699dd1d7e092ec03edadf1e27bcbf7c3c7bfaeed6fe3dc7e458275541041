flowchart LR
    Renderer["<b>Renderer (React)</b><br/>━━━━━━━━<br/>TrpcProvider"]
    Electron["<b>Electron host</b><br/>@slayzone/app<br/>━━━━━━━━<br/>EmbeddedServerHandle<br/>ElectronAPI (preload)"]
    SideCar["<b>Side-car</b><br/>@slayzone/server<br/>━━━━━━━━<br/>startServer(<br/>&nbsp;&nbsp;StartServerConfig<br/>) → ServerHandle<br/>appRouter"]
    DB[("<b>SQLite (WAL)</b><br/>━━━━━━━━<br/>SlayzoneDb<br/>(@slayzone/platform)")]

    Renderer == "tRPC over WS<br/>typed by AppRouter" ==> SideCar
    Renderer -. "legacy IPC<br/>typed by ElectronAPI" .-> Electron
    Electron -- "spawns + supervises<br/>via EmbeddedServerHandle" --> SideCar
    Electron --> DB
    SideCar --> DB

    classDef new fill:#1e3a5f,stroke:#4a9eff,stroke-width:2px,color:#fff
    classDef existing fill:#2a2a2a,stroke:#888,color:#ddd
    classDef shared fill:#3a2f1e,stroke:#d4a04a,color:#fff
    class SideCar new
    class Renderer,Electron existing
    class DB shared
