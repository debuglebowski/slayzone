# Slice 3/8 — notify subs (P20) — DONE

**Commit:** `3ae97bd8` (direct to main, no PR)
**Message:** feat(transport): notify subs (P20 — tasks-changed/settings-changed via notifyEvents EventEmitter; replaces 'tasks:changed'/'settings:changed' webContents.send broadcasts)

## What shipped
Mirror two IPC broadcasts as tRPC subscriptions. **Dual-emit, server-only** — IPC stays live, tRPC subs added alongside. Zero renderer mutation (slice 5 cuts over).

### Files (7)
- `transport/server/routers/notify.ts` **(new)** — `onTasksChanged` + `onSettingsChanged` subs (`observable<void>`, mirror agent-turns shape)
- `transport/server/app-deps.ts` — `NotifyEventMap` + `setNotifyEvents`/`getNotifyEvents` injection (mirrors setChatDeps/setTaskDeps)
- `transport/server/router.ts` — register `notify: notifyRouter`
- `transport/server/index.ts` — export `setNotifyEvents`/`getNotifyEvents`/`NotifyEventMap`
- `apps/app/main/notify-renderer.ts` — own `notifyEvents = new TypedEmitter<NotifyEventMap>()`; `notifyRenderer()` dual-emits (emit + broadcastToWindows)
- `apps/app/main/index.ts` — import notifyEvents, `mod.setNotifyEvents(notifyEvents)` at boot, dual-emit in `notifyTasksChanged`
- `apps/app/main/export-import.ts` — dual-emit at 2 import sites

## Chain
CLI → SQLite → `POST /api/notify` → `notifyRenderer()` → `notifyEvents.emit('tasks-changed')` → tRPC sub fans out to all WS conns → (slice 5) renderer `loadData`. in-flight guard in loadData prevents redundant refresh.

## Adapted from ref 8ab75b7e (NOT replayed)
Ref tree diverged 4wk: no `routers/app.ts`/`getAppDeps` now (split into focused routers). Ref also cut over renderer (preload/PanelsSettingsTab/usePanelConfig/api.ts) + missed migrating `useTasksData` → would regress board auto-refresh on current tree. Prose said "No renderer mutation (slice 5)". → server-only dual-emit, matches sibling slices' coexistence + "never drop functionality". Used `TypedEmitter` (current convention) not ref's raw EventEmitter. Subs at top-level `notify` (no `app` router exists).

## Verify (green)
- typecheck ✓ transport + apps/app (node + web projects, exit 0)
- lint ✓ my 7 files (0 problems)
- build ✓ full prod electron-vite (32.8s, cross-pkg imports resolve)
- e2e ✓ 7 passed — core/01-smoke (boot wiring) + core/04-tasks-crud (IPC refresh intact, no regression)
- unit tests: blocked by pre-existing better-sqlite3 ABI mismatch (NODE_MODULE_VERSION 145 vs 137) — env, not my code

## Shared-tree safety
Committed via temp-index + my-only blobs + CAS update-ref + reset (peers' uncommitted ai-config/pty WIP untouched; content-guard confirmed patch = my lines only).
