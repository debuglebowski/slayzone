# Phase 0 — Remaining failing tests (post round 13)

## ✅ Round 13 fixed
- `32-git-diff-panel` ALL 11 tests — `polling`: explicit refresh after writeFileSync; `chip row`: `git clean -fd` to clear untracked test residue
- `42-terminal-tab-isolation:26` — Cmd+K isn't a "new tab" shortcut anymore (it's search). Rewrote test to use `tabs.create` API directly; semantically asserts isolation
- `24-browser-panel:145` Alt+Shift+Click — passes iso, was full-suite cascade

## Still failing — sorted by effort

### 🔴 Large (3)
- `93-artifacts-panel` ×7 — test-state-dependent ordering (rename → delete → empty folder visibility)
- `61-web-panel-handoff-routing:182` — uses stale `<webview>` selector; web panels migrated to WebContentsView (rewrite)
- `71-webview-popup-handling:540/696` — same root as 61

### 💀 Very Large (3)
- `90-terminal-fd-limit:34` RLIMIT
- `72-browser-view-lifecycle:184` Bug 1
- `77-browser-view-focus:89` glow

## Tally
🟢 0 · 🟡 0 · 🔴 3 · 💀 3 → **6 rows / ~10 tests** still failing

Cumulative: **80 → ~10 (~88% reduction)**.
