# Slice 3/8 — tags tRPC router (P2 pilot) — DONE

## Commit
`609bc0a24e24ae21fafd8924331b0c429f22a912` — direct to main
`feat(transport): tags router (P2 — pilot)`

## Delivered
- `packages/domains/tags/src/server/tag-store.ts` — pure SQL store (8 fns)
- `packages/domains/tags/src/server/index.ts` — re-exports
- `packages/shared/transport/src/server/routers/tags.ts` — 8 procedures (5 mut + 3 query), zod-validated
- `appRouter` mounts `tags: tagsRouter`
- `@slayzone/transport` deps += `@slayzone/tags`
- `tags/main/handlers.ts` refactored — delegates to store, channel contract unchanged

## Procedure ↔ channel map
| tRPC | IPC channel (preserved) |
| --- | --- |
| tags.list | db:tags:getAll |
| tags.create | db:tags:create |
| tags.update | db:tags:update |
| tags.delete | db:tags:delete |
| tags.reorder | db:tags:reorder |
| tags.getForTask | db:taskTags:getForTask |
| tags.getAllTaskTagIds | db:taskTags:getAll |
| tags.setForTask | db:taskTags:setForTask |

## Verification
- `pnpm typecheck` — all 38 packages green
- `pnpm lint` — 0 errors (301 pre-existing warnings)
- `pnpm build` — full prod build OK
- `pnpm test:e2e` — 777 passed, 0 failed, 111 skipped

## Constraints honored
- ipcMain.handle delta: 0 (8 → 8 channels intact)
- Renderer untouched — 11 `window.api.tags.*` / `taskTags.*` sites preserved (slice 5)
- Reference branch `origin/phase-0-path-port-hardening` not merged (read-only ebe79193)

## Deferred (intentional)
- `tags/server/events.ts` EventEmitter — no tRPC callers yet; revisit at slice 5 cutover (automations engine still receives `db:taskTags:setForTask:done` via ipcMain.emit from IPC handler path)
- Direct tRPC e2e — router is dead code until slice 5

## Pattern established for P3–P8
1. Extract pure store under `<domain>/src/server/`
2. Add `./server` to domain package.json exports
3. Refactor IPC handler to delegate to store (no SQL dup)
4. Mirror as `shared/transport/src/server/routers/<domain>.ts` w/ zod inputs
5. Mount in appRouter
6. Add `@slayzone/<domain>` workspace dep to transport package

