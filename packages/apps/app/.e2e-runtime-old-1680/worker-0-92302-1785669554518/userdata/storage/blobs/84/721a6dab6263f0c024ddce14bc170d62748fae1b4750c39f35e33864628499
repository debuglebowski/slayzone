# SlayZone Env Vars — Standalone Hub & Runner

Deployment-relevant vars for standalone (non-supervised) hub + runner, plus every var surfaced by the full 4-cluster deep-dive. Dev/build/e2e/internal vars summarized at bottom.

**Precedence everywhere:** `env var > SLAYZONE_RUNNER_CONFIG file (runner) > ~/.slayzone/config.json > generated/default`

**CWD-anchoring = default-only, NO new knob.** Standalone (non-supervised) hub/runner default `SLAYZONE_HOME_DIR` + `SLAYZONE_STORE_DIR` + `SLAYZONE_RUNNER_CREDENTIALS_DIR` to CWD. "Relocate everything" = launch from another dir (`cd /srv/slayzone && slayzone-hub`) — no env var. Fine-grained overrides stay for the niche "config here, DB there" split.

**Minimize legend** (does the operator hold info the process can't derive at boot?):
- 🟢 **keep** — genuinely operator-only info
- 🔵 **derive** — process can compute it; keep only as optional override
- 🟠 **hide** — subsumed by a richer input; code stays, drop from docs
- 🔴 **delete** — derivable/dup/dead/redundant; remove the knob entirely

## Operator-facing + deploy-adjacent (sorted: minimize action, then category)

| Minimize | Category | Var | Purpose | Default | config.json key | Anchored to |
|---|---|---|---|---|---|---|
| 🟢 keep | Control | `SLAYZONE_SUPERVISED` | `=1` → Electron host owns env; THE standalone discriminator | unset (standalone) | — | — |
| 🟢 keep | Hub | `SLAYZONE_DB_PATH` | Exact SQLite file path; REQUIRED in supervised | — (composed from STORE_DIR+name) | `dbPath` | — (absolute) |
| 🟢 keep | Hub | `SLAYZONE_STORE_DIR` | State root: DB, logs/, diagnostics, artifacts | **CWD** (standalone); `getStateDir()` (supervised) | — | **CWD** |
| 🟢 keep | Hub | `SLAYZONE_PORT` | Client-facing tRPC/HTTP port | OS-assigned | `port` | — |
| 🟢 keep | Hub | `SLAYZONE_HOST` | Bind addr for /trpc (client) | `127.0.0.1` | — | — |
| 🟢 keep | Hub | `SLAYZONE_RUNNER_TRANSPORT_PORT` | `/runners` wss listener port (stable) | OS-assigned (stable via DB) | `runnerTransportPort` | — |
| 🟢 keep | Hub | `SLAYZONE_RUNNER_TRANSPORT_HOST` | `/runners` bind addr | `0.0.0.0` | — | — |
| 🟢 keep | Hub | `SLAYZONE_HUB_TLS_PORT` | TLS `/trpc` https port (multi-hub) | OS-assigned | — | — |
| 🟢 keep | Hub | `SLAYZONE_RUNNER_TRANSPORT_SECRET` | HMAC secret; runner-auth + token mint | gen+persist 256-bit | `runnerTransportSecret` | CWD (config.json) |
| 🟢 keep | Hub | `SLAYZONE_HUB_PUBLIC_URL` | Public base URL advertised to remote MCP/hooks | derived loopback | `publicUrl` | — |
| 🟢 keep | Hub | `SLAYZONE_HOME_DIR` | config.json + hooks root — fine-grained override | **CWD** (standalone); `$HOME/.slayzone` (supervised) | — | **CWD** |
| 🟢 keep | Runner | `SLAYZONE_JOIN_TOKEN` | First-contact enroll token — embeds hubUrl+cert+secret | — | `joinToken` | — |
| 🟢 keep | Runner | `SLAYZONE_RUNNER_ALLOWED_ROOTS` | FS path-jail (security sandbox); every fs/git/proc path checked | **none → fs refused** (fail-closed) | — | — |
| 🟢 keep | Runner | `SLAYZONE_RUNNER_NAME` | Display name on hub (dedup key) | hostname | `runnerName` | — |
| 🟢 keep | Runner | `SLAYZONE_RUNNER_CREDENTIALS_DIR` | Post-enroll cred store dir — fine-grained override | **CWD/runner** (standalone); `~/.slayzone/runner` (supervised) | — | **CWD** |
| 🟢 keep | Runner | `SLAYZONE_RUNNER_CONFIG` | Path to per-host JSON config file | — | — | — |
| 🟢 keep | Runner | `SLAYZONE_RUNNER_HEARTBEAT_MS` | Heartbeat interval (ms) — low-priority tuning | transport default | — | — |
| 🔵 derive | Hub | `SLAYZONE_HUB_AUTH_REQUIRED` | Gate client /trpc on auth. **Derive default: non-loopback HOST ⟹ on.** Keep as optional override | derive from HOST | — | — |
| 🟠 hide | Runner | `SLAYZONE_HUB_URL` | `ws(s)://…/runners` — subsumed by join token | from token | `hubUrl` | — |
| 🟠 hide | Runner | `SLAYZONE_HUB_CERT_SHA256` | TLS cert pin — subsumed by join token | from token | — | — |
| 🔴 delete | Control | `SLAYZONE_ROOT` | *(proposed — DON'T introduce)* redundant: CWD-anchor default + `cd` already relocates the whole install; no knob needed | n/a | — | — |
| 🔴 delete | Hub | `SLAYZONE_ALLOW_PLAINTEXT_CREDENTIALS` | Derive from cipher `isEncryptionAvailable()`; headless hub has no cipher anyway | off (on if NODE_ENV=test) | — | — |
| 🔴 delete | Hub | `SLAYZONE_DB_DIR` | Dup of STORE_DIR. ⚠️ precedence inverted vs artifacts/shared.ts | falls to STORE_DIR | — | — |
| 🔴 delete | Hub | `SLAYZONE_DB_NAME` | Subsumed by DB_PATH; filename never varies on a server | `slayzone[.dev].sqlite` | — | — |
| 🔴 delete | Hub | `SLAYZONE_RUNNER_TRANSPORT_BASE_URL` | Loopback constant; runners use bearer not cookies → inline it | `http://127.0.0.1:8788` | — | — |
| 🔴 delete | Runner | `SLAYZONE_RUNNER_CAPABILITIES` | Stored, NEVER enforced → drop until enforcement exists | `pty,git,fs,proc` | — | — |

## Dead / bug (verified — remove or fix, NOT deploy knobs)

| Minimize | Var | Finding |
|---|---|---|
| 🔴 delete (dead) | `SLAYZONE_SERVER_PORT` | `getServerPort()` defined + barrel-exported, zero callers. Superseded by SLAYZONE_PORT → remove var+helper |
| 🔴 delete (dead) | `SLAYZONE_MCP_HOST` | Only written to docker/ssh child env; no reader (CLI hardcodes 127.0.0.1) → remove 2 write lines |
| 🔴 delete (dead) | `SLAYZONE_RUNNERS_ENABLED` | Only set in one test; never read in prod. Hub always runs gateway → remove from test |
| 🔴 delete (dead) | `SLAYZONE_HOOK_NAME` | Not an env var — JS `const='slayzone-notify'`. Mis-catalogued → drop from list |
| 🐛 bug | `SLAYZONE_AGENT_MODE` | 1 reader (`cliAuthor()`), 0 setters → CLI artifacts always tag `user`. Likely should read SLAYZONE_AGENT_ID |
| 🐛 bug | `SLAYZONE_DB_DIR` ↔ `STORE_DIR` | Precedence inverted: hub `STORE_DIR ?? DB_DIR`, artifacts `DB_DIR ‖ STORE_DIR` → split dirs if both set |

## Not operator-facing (code stays, omit from deploy docs)

- **Machine-injected per-run** (agent/hooks/session): `SLAYZONE_HUB_TOKEN` (per-task bearer, ≠ JOIN_TOKEN), `SLAYZONE_AGENT_HOOK_URL`, `SLAYZONE_AGENT_ID`, `SLAYZONE_SESSION_ID`, `SLAYZONE_PROJECT_ID`, `SLAYZONE_TASK_ID`, `SLAYZONE_MCP_PORT`
- **Fork sidecar only:** `SLAYZONE_RUNTIME_DIR`, `SLAYZONE_HOST_CAP_URL`, `SLAYZONE_HOST_REST_URL` (supervised host bridge)
- **e2e-only:** `SLAYZONE_USER_DATA_DIR`, `SLAYZONE_BOOT_LOG_PATH`, `SLAYZONE_E2E_ALLOW_RUNNER`, `SLAYZONE_E2E_INSTALL_HOOKS`, `*_SETTINGS_PATH`/`*_HOOKS_PATH`/`OPENCODE_PLUGIN_PATH` (test redirects)
- **Dev/build:** `SLAYZONE_DEV`, `SLAYZONE_REACT_DEV`, `SLAYZONE_PROFILE`, `SLAYZONE_CHROMIUM_PROD`, `SLAYZONE_DEBUG_BOOT`, `SLAYZONE_REGISTER_DEV_PROTOCOL`, `SLAYZONE_SIDECAR_HOT_RESTART`, `SLAYZONE_SHELL_DEV_SERVER`, `SLAYZONE_DISABLE_CLONEFILE`, `SLAYZONE_GC_*`, `*__` (build-time defines: `SERVER_BUILT_AT__`, `SERVER_COMMIT__`, `CHROMIUM_PROD__`, `PROFILE__`)

## Net effect

Operator surface: ~27 → **17 keep + 1 derive-override**. Rest deletes (6 incl. never-introduced ROOT), hides (2), or is dead (4).

CWD-anchoring adds ZERO new vars — it's a default change on 3 existing vars (`SLAYZONE_HOME_DIR`, `SLAYZONE_STORE_DIR`, `SLAYZONE_RUNNER_CREDENTIALS_DIR`), gated on `!SLAYZONE_SUPERVISED`. Relocate = `cd`.

Deploy collapses to:
- **Hub:** `SLAYZONE_RUNNER_TRANSPORT_SECRET` (auto-gen) + optional port/host/public-url
- **Runner:** `SLAYZONE_JOIN_TOKEN` + `SLAYZONE_RUNNER_ALLOWED_ROOTS`

**CWD-anchored (bold) = the 3 this task retargets:** `SLAYZONE_HOME_DIR`, `SLAYZONE_STORE_DIR`, `SLAYZONE_RUNNER_CREDENTIALS_DIR` (+ raw-homedir credential-store gap to fix).

⚠️ All delete/derive/hide = behavior/code changes, SEPARATE from the CWD-anchoring task.
