# Slice 5 — Renderer tRPC cutover — running log

Renderer cutover DONE (24 batches) + perf regressions fixed + lint guard. App works in daily use.

## P19 (e2e validation + window.api→vanilla swap) — status
- E2E swap DONE: fixtures (64faab4e) + per-spec terminal/git/browser/core/perf (01fb9035). Verified clean (zero tRPC errors). Seed via window.getTrpcVanillaClient(); refreshData kept.
- Suite run surfaced ~26 slice-5 CUTOVER regressions (the suite was deferred during the cutover, so these were uncaught).
- REAL BUG fixed (ae3d4f1c): git-diff-store served a stale react-query-cached working-diff (global 5s staleTime) → "git diff slow" + e2e fails. getWorkingDiff now staleTime:0. Cleared the git-diff cluster (git/ 179 passed / 2 failed, was ~16 failing).
- REMAINING ~10 scattered failures handed off to sub-task 190ea37d (likely-real: terminal/22 temp-task-close, git/35:328 auto-worktree; likely-env: real-CLI/slay-CLI/hooks/webview tests).

## Key learnings (memory project_trpc_usemutation_unstable_deps_loops + this log)
- Real-time reads (working diff, etc.) must bypass react-query cache: staleTime:0.
- useMutation/useQuery OBJECT in hook deps → infinite WS loop (lint:trpc-deps guards).
- Cross-transport: seed via tRPC → UI query cache stale → call __slayzone_refreshData.
- WS needs ?windowId=; TrpcProvider outermost; initTrpcClient before mount; full pnpm lint/typecheck chromium-fork-broken (use scoped).

## Continuation: sub-task 190ea37d
Finish P19 remaining-failure triage + the other slice-5 gaps (slice-3 router gaps for zero-window.api; 4 orphaned task unit tests). Full prompt in that subtask's description.
