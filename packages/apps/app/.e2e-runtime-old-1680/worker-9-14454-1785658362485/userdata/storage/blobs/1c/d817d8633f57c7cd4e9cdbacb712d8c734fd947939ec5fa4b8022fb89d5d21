# CAP-7 Capacity Management — fix brief

Vanta control CAP-7 (ISO 27001 A.8.6) has 2 failing tests on Azure. Both are missing-alert problems — no architecture changes, just metric alerts wired into our existing action group.

**Subscription:** `a9049f9f-3d1b-43f0-9278-7f1d603f6a00` (Normain default)
**Action group to use:** `ag-alerts-normain-prod-01` (already routes to `alerts@normain.com`) — reuse for all new alerts, prod and dev.

---

## Task 1 — VMSS CPU alerts

**Vanta test:** `Virtual machine scale set CPU monitored (Azure)`
Add a `Percentage CPU` metric alert on each AKS node-pool VMSS. One alert per VMSS resource — covers all current and future instances.

| VMSS | Resource group |
|---|---|
| `aks-default-31982391-vmss` | `MC_rg-normain-prod-01_aks-normain-prod-01_swedencentral` |
| `aks-default-25586624-vmss` | `MC_rg-normain-dev-20_aks-normain-dev-20_swedencentral` (dev — only if in audit scope) |

Threshold suggestion: avg > 80% over 15 min, severity 2.

## Task 2 — Storage account availability alerts

**Vanta test:** `Storage Accounts health monitored (Azure)`
Add an Availability metric alert on each of the 10 storage accounts below. Use the existing `storage-availability-alert-prod-01` (on `stwinvmnormainprod01`) as the template.

| Account | RG |
|---|---|
| `rpsnormainprod01` | rg-normain-prod-01 |
| `dssnormainprod01` | rg-normain-prod-01 |
| `auinormainprod01` | rg-normain-prod-01 |
| `normaintfappproduction` | normain-tf-app-production |
| `normainfglobal` | normain-tf-global |
| `normaintfidentity` | normain-tf-identity |
| `rpsnormaindev20` | rg-normain-dev-20 |
| `dssnormaindev20` | rg-normain-dev-20 |
| `auinormaindev20` | rg-normain-dev-20 |
| `normaintfappdevelopment` | normain-tf-app-development |

Threshold suggestion: avg Availability < 99 over 15 min, severity 2.

## Task 3 — Cleanup

There are 6 disabled alerts in `rg-normain-prod-01` named `ca-*-normain-prod-01` targeting a Container App `aca-normain-backend-api-prod-01` that does not exist. Delete them.

---

## After deployment

1. In Vanta → Integrations → Azure: click "Sync now" (or wait up to 24 h).
2. Verify CAP-7 goes from 10/12 → 12/12.
3. Confirm: action group fired a confirmation email on the first alert eval (sanity-check the routing).

## Open decisions for me to confirm before you start

- **Dev in scope?** If `rg-normain-dev-20` and dev TF-state RGs are NOT in audit scope, skip those rows.
- **Threshold values:** OK with the 80% / Availability<99 / sev 2 suggestions, or do you want something tighter?
- **Where to define the alerts** — direct `az` / portal, or add to Terraform if our monitoring stack lives there?
