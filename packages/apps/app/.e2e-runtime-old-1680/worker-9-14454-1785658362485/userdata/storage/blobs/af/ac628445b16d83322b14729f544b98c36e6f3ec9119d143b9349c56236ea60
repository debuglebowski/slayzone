# Slice 6 — @slayzone/server fully functional (dark) — COMPLETE

All 7 sub-slices done on working tree (uncommitted, awaiting commit approval). Standalone smoke: **13/13 PASS**.

## What landed

### 6b1 — namedTxn
- `@slayzone/transport/txns` (new export): domain txn composition + dup-key assert, shared by app DB worker + sidecar
- `SyncSlayzoneDb.namedTxn` dispatches against it (was: unconditional throw → every mutation broken standalone)

### 6a — MCP + REST + mcp-tools → `@slayzone/transport/server/http/`
- `createMcpRestApp(deps)` (mux-ready) + `startMcpServer(deps)` (Electron path: preferred-port, EADDRINUSE fallback, `mcp_server_port` write, `__mcpPort`)
- `RestApiDeps` capability slots: `pty / browser / processes / artifactExport / windowActions / menu / legacyBroadcast / taskBus / terminalStateBridge` — absent slot → 501 "not available in standalone server"
- App injects all slots via `apps/app/src/main/mcp-rest-deps.ts` (same singletons as before — behavior-identical)
- `markTabSpawned`/`markTabHibernated`/`listHibernatedSessionIds` → task-terminals/server (electron re-exports)
- agent-hook tests modernized (53/53) — old mocks asserted pre-provenance updateTask flow (rotted, orphaned from CI)

### 6b2 — AutomationEngine → automations/server
- `start(bus: AutomationTriggerBus)` structural param (main passes ipcMain verbatim); engine.test 31/31 green
- Documented latent gap: tRPC `tags.setForTask` never emits the tag-trigger channel (pre-existing, slice-7 item)

### 6b3 — new `@slayzone/processes` domain
- process-manager + pid-stats moved from app main; `ProcessBroadcastWindow` structural type; `getEnrichedPath` now from terminal/server

### 6d1 — sidecar composition root + mux + migrations
- `composition.ts`: populates ALL transport registries — real impls (task ops + runtime adapters, integrations, feedback, processes, own event buses) + fail-loud stubs for electron-shell keys + graceful read-path defaults
- AutomationEngine constructed NEVER started (no cron double-fire vs live Electron host)
- Single-port mux: `/health` (pre-express) + `/api/*` + `/mcp` + `/trpc` WS
- Standalone boot runs all 145 migrations + normalize + syncTerminalModes via new `@slayzone/transport/db-bootstrap` (migration stack moved out of app; DB worker imports from it now); supervised boots skip schema entirely
- `__mcpPort` set, `mcp_server_port` NEVER written (main's stays discoverable while dark)

### 6c — terminal pty/chat electron inversion + move
- Stage 1: `pty-host.ts` bridge (getAllWindows/getFocusedWindow/isDarkTheme/bus) replaces ALL electron value-imports in pty-manager, pty-store, chat-handlers, chat-transport-manager, chat-queue-handlers; electron entry configures real impls at import time → byte-identical in app
- Stage 2: 8 modules + 6 tests moved to `terminal/src/server/runtime/`; electron/ keeps handlers.ts (IPC glue) + usage.ts (electron net) + index.ts (bridge config + re-exports); `IpcMainLike` structural type
- Terminal e2e gate: 83 passed / 9 failed — ALL 9 verified pre-existing (worktree differential vs clean main) or established late-run flake; ZERO new failures

### 6d2 — full sidecar wire
- setPtyDeps + setChatDeps wired from terminal/server barrel; REST pty slot + terminalStateBridge live → `/api/pty` 200, `pty.exists` answers
- **Bundle switched ESM→CJS (`dist/bin.cjs`)**: ESM imports of externals ignore NODE_PATH — packaged resolution was impossible; CJS honors walk-up AND NODE_PATH. Validated via simulated packaged layout (Resources/server/bin.cjs + app.asar.unpacked/node_modules) → health 200
- node-pty external + dep; supervisor spawns bin.cjs (7/7 tests incl parent-death against real bundle)

## Latent bugs found + fixed
1. **Packaged sidecar could never start** (only bin.js copied; no node_modules on walk-up; masked by dark launch). Fix: NODE_PATH=app.asar.unpacked/node_modules in supervisor spawn + CJS bundle.
2. **`integrations/server/sync.ts` statically imported `@slayzone/terminal/electron`** — dragged pty cluster + electron npm shim into the sidecar bundle → instant crash. Fixed (terminal/server) + guard extended to catch the whole class (static value imports of any domain /electron entry from server-side dirs, type-only + dynamic imports exempt).
3. processes domain tsconfig needed the TxnRegistry aggregator include (caught by root typecheck).

## Verification matrix
- Root typecheck: 0 errors · prod build: green · boundary guard (extended): green
- Standalone smoke (fresh store): 13/13 — health, tRPC CRUD via namedTxn, stub errors, pty wired, REST, 501 degradation, MCP handshake
- Packaged-layout sim: health 200
- Supervisor unit: 7/7 · transport vitest: 55/55 · terminal runtime units: adopt-pty 3/3, createpty-resolver 6/6, warm-process 11/11, session-registry 6/6, CTM 44/45 (1 pre-existing, identical on main)
- e2e: REST/hooks/tabs 9/9; terminal suite 83 pass, failures ⊆ main's pre-existing set (differential-verified twice via clean worktrees)
- Pre-existing fail buckets NOT regressed: rest-api/tasks + mcp-tools electron-harness tests (async-DB rot, documented in run-all.sh), 34-cli-pty-start cold-spawn, 28-detect-click, 50-screenshot

## Slice-7 handoff notes
- Flip = renderer WS → sidecar + local/remote toggle; shell-router split for electron-only AppDeps keys (stubs mark exactly what must move)
- DB single-owner cutover (sidecar currently opens 2nd WAL handle, fine while dark)
- tags.setForTask tRPC trigger gap; backup/leaderboard/export-import relocation; pty CREATE still needs a window (renderer-driven) — headless spawn is the remote-shells item
- run-all.sh: fixed ~20 stale terminal/src/main paths (those suites now actually run; chat-handlers shows its real async-DB rot: 4/14)
