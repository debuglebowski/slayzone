# Slice 5 — Renderer tRPC cutover — progress

Mode: autonomous, live tree, direct-to-main per verified batch (signed commits).
Verify per batch: scoped tsgo (domain + task/transport + app) + scoped eslint + rewritten unit tests. Full `pnpm lint`/`pnpm typecheck` unusable (chromium/ fork pollution) — scoped only. Build checkpoint after leaf tier: green.

## Batches (pushed commits)
- [x] P0 infra — tanstack switch + e2e vanilla client + dataRoot fix — 34f40031
- [x] P1 tags — 97447fe1
- [x] P2 agent-turns (query+subscription) — 4e948ee7
- [x] P3 usage-analytics — 2fed684f
- [x] P4 automations — c32bc599 (batch)
- [x] P5 test-panel — c32bc599 (batch)
- [x] P6 task-artifacts (+artifact-store version-type narrowing) — c32bc599 (batch)
- [x] P7 file-editor — c32bc599 (batch)
- [ ] P6b feedback router + dialog (NEEDS server router first)
- [ ] P8 settings  / P9 projects / P10 task-browser / P11 worktrees (in progress)
- [ ] P12 ai-config / P13 task-terminals / P14 terminal ops / P15 terminal subs
- [ ] P16 tasks (board + refreshData) / P17 app renderer / P18 remaining / P19 e2e

## Residuals left on bridge (allowlist — no router / electron-native)
- window.api.files.getDropPaths (file-editor, task-artifacts) — preload webUtils, intended to stay
- window.api.automations.onChanged — no tRPC subscription (slice-3 gap); IPC listener kept, rewired to invalidate

## Unclarities (for final report)
1. `pnpm lint` + `pnpm typecheck` (root) both broken by chromium/ fork tree (mojo-bindings unresolved, eslint scans 70GB). Whole-repo gates need chromium/ ignored.
2. 1Password ssh signing works non-interactively — signed commits OK.
3. Stale .claude/worktrees/ copies break `vitest` globs — must --exclude '**/.claude/worktrees/**'.
4. Slice-3 gaps surfaced: automations router has no onChanged subscription; feedback has no router at all (P6b builds it).
5. Full e2e (hours) deferred to a late consolidated checkpoint — per-batch verified via typecheck/scoped tests/build.
