# Phase 0 — Remaining failing tests (post round 9)

## ✅ Round 9 fixed
- `30-terminal-core-behavior:276` Cmd+Alt+R — `expect.poll` for buffer assert
- `94-artifact-html-preview` (4 tests) — sandboxed iframe click via JS evaluate

## Investigated (round 10) — needs deeper code work

### `14-project-settings:454` 🟡

Tried: switch to robust `openProjectSettings` fixture, replace `clickSettingsTab` retry-loop with direct click. Still fails — `page.waitForTimeout: Target page, context or browser has been closed`. Page crashes during initial dialog open, before any click on columns tab. Likely a prior test leaves window state that crashes when re-entering Settings dialog from sidebar in iso run.

### `22-terminal-mode-switching:140` 🟡

Tried: explicit initial `terminalMode: 'claude-code'`, longer timeouts, direct option click bypass of switchTerminalMode helper, DB-backed assertion poll. Still fails — page closes when clicking 'codex' option for a temporary task. Suggests handleModeChange triggers cleanup that deletes the temp task's tab/PTY mid-flight, taking the page with it. Real app bug — non-temp tasks switch fine.

## Still failing — sorted by effort

### 🟡 Medium (2)
- `14-project-settings:454` delete column — page closes opening Settings in iso  
- `22-terminal-mode-switching:140` temp tasks — handleModeChange + temp task = page crash

### 🔴 Large (7)
- `93-artifacts-panel` ×7 (down from 11)
- `33-pty-respawn-forced` ×4 (cascade)
- `42-terminal-tab-isolation:26` Cmd+K
- `24-browser-panel:145` Alt+Shift+Click
- `61-web-panel-handoff-routing:182`
- `71-webview-popup-handling:540/696`
- `32-git-diff-panel:171/219`

### 💀 Very Large (3)
- `90-terminal-fd-limit:34` RLIMIT
- `72-browser-view-lifecycle:184` Bug 1
- `77-browser-view-focus:89` glow

## Tally
🟢 0 · 🟡 2 · 🔴 7 · 💀 3 → **12 rows / ~20 tests** still failing

Cumulative: **80 → ~20 (75% reduction)**. All 🟢 cleared. 🟡 reduced from 5 → 2 (the remaining 2 are real app bugs, not test-side fixes).
