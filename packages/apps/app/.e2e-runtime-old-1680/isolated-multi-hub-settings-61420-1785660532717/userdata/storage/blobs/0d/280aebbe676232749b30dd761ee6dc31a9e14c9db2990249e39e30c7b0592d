# Open Decisions — SLA / DPA / Subprocessor consolidation

_Companion to the structure plan (`Drive structure proposal`). Tracks the content/legal decisions, not the folder layout. As of 2026-06-03._

**Status:** 🟢 recommendation ready (needs your nod) · 🟡 needs your input · ⏸ deferred

## Document relationships (reference — settles "is the SLA part of the DPA?")

| Doc | Role | Notes |
|---|---|---|
| Main Agreement | Umbrella contract for direct/enterprise deals | |
| Attachment 1 — Terms of Service | Commercial/service terms (enterprise) | |
| **Attachment 2 — SLA** | Uptime commitment | **Separate document** |
| **Attachment 3 — DPA** | Data-processing terms (GDPR Art. 28) | **Separate document; the subprocessor list is embedded *inside* it as an appendix** |
| Terms of Use (Product) + Privacy + Cookie | Self-serve SaaS click-through terms | |

→ **SLA is NOT part of the DPA** (sibling attachments). **Subprocessor list IS part of the DPA.** A whole-package snapshot holds the SLA and the DPA side by side.

## Q4 — SLA versions ✅ resolved
**Principle:** a template snapshot is cut when the **offer/agreements change materially** — not per customer, not per minor edit. (~23 "Attachment 2 – SLA" docs exist, but most are per-customer copies → excluded.)
**Snapshots to keep** (the distinct content versions):
- **2026-05-29 - DRAFT** — 99%, SOC2-aligned cleanup
- **2025-12-17** — 99%, KPMG final → current `- LATEST`
- one **90%** bare-date historical (the last 90% template)
Per-customer copies stay frozen in their customer folders; older scattered copies → `99 - Old`. SLA = Attachment 2, bundled into each package snapshot alongside the DPA (not inside it).

## Q3b — Cookie Policy: product vs website? ✅ resolved
**Decision:** split into two — **Cookie Policy – Product** (→ `02 - SaaS default agreements / 03 - Cookie Policy`) and **Cookie Policy – Website** (→ `04 - Marketing / 01 - Website`) — so the structure is uniform regardless of whether content currently differs. (Prompt: the single Aug-2024 policy claims to cover "website **and** platform," but every cookie it lists — Google, HubSpot, Intercom — is website/marketing.)

## Q1 — Hosting DPA ✅ resolved
The 3 "hosting DPA" files resolve as follows:

| Doc | Was | Decision |
|---|---|---|
| `11xa` — Normain-hosted on Azure (older + filled list) | dedicated Normain-hosting template | **Retire → `99 - Old`.** This business now runs through **Marketplace** (which has its own Marketplace DPA), so a standalone Normain-hosting template is no longer needed. |
| `1it8` — "Normain hosting" filename, but body = generic "Normain's provision of **The Platform**" (newest, empty list) | mislabeled | **Repurpose as the SaaS DPA** → `02 - SaaS default agreements / 04 - DPA`. Fill its empty subprocessor appendix. |
| `14JrU` — Customer-hosted (customer's own cloud) | customer-self-hosting template | **Keep** as the Customer Cloud Hosting DPA → `00 - Customer agreement Templates / 02 - Customer Cloud Hosting Template`. |

Signed PDF in InfoSec `2. DPA` stays untouched (frozen evidence). Net: we keep **two** DPA lineages — a **SaaS DPA** (`1it8` basis) and a **customer-self-hosting DPA** (`14JrU`) — each in its own folder; the Normain-hosted-on-Azure variant is retired.

## Q3 — Terms of Use vs DPA — what's praxis 🟢
- **Self-serve SaaS:** customer click-accepts **Terms of Use (Product)** + Privacy + Cookie. Terms of Use *is* the contract.
- **Enterprise/negotiated** (Marketplace, hosting): a **negotiated Main Agreement** (or Marketplace Order + Microsoft Standard Contract) is the contract, with its own commercial terms (your **Attachment 1 – Terms of Service**). The click-through Terms of Use does **not** apply — the negotiated agreement replaces it.
- **The DPA does NOT fill the Terms role.** It's a separate **data-protection** layer (GDPR Art. 28) that sits *under* whichever contract applies. Terms/Main Agreement = the commercial+service deal; DPA = how personal data is processed.

→ So Terms of Use is the self-serve contract; enterprise uses the Main Agreement (+ Attachment 1); the DPA is an annex in both, never a substitute for Terms.

**Applicability matrix:**

| Document | How it's accepted | Self-serve SaaS | Enterprise – Marketplace | Enterprise – Hosting |
|---|---|---|---|---|
| *Governing commercial contract* | Accepted by the org | Terms of Use (Product), click-accept | Order + Microsoft Standard Contract | Main Agreement + Attachment 1 |
| **Terms of Use (Product)** | Contract | ✅ it *is* the contract | ❌ MS Standard Contract governs | ❌ Main Agreement governs |
| **Privacy Policy (Product)** | **User notice — NOT a contract term** | ✅ | ✅ | ✅ |
| **Cookie Policy** | **User/browser consent — NOT a contract term** | ✅ | ✅ | ✅ |
| **DPA** | Legal requirement (GDPR Art. 28) | ✅ click-through DPA — create in `02/04 - DPA` (not plan-dependent) | ✅ Marketplace DPA (14-day notice) | ✅ Attachment 3 hosting DPA (30-day notice) |
| *SLA (Attachment 2)* | Commercial promise — **plan-dependent** | ✅ create in `02/05 - SLA` (applies on eligible tiers; best-effort otherwise) | ✅ publisher SLA / Attachment 2 (99%) | ✅ Attachment 2 (99%) |

Notes:
- Privacy & Cookie are accepted by individual **users**, not the enterprise org — verified: the Main Agreement lists only Terms / SLA / DPA as the contract, and Attachment 1 routes all personal-data processing to the **DPA**. → both live in `02 - SaaS default agreements` as published notices, never inside a contract snapshot.
- **DPA vs SLA differ in kind for SaaS:** the **DPA is a *legal* requirement** (needed whenever Normain processes personal data on a customer's behalf — independent of price/plan), so it applies to SaaS and just needs a click-through form. The **SLA is a *commercial* promise** (uptime + credits) — genuinely a **plan question**: typically paid tiers only; free/experimentation has none.
- Privacy Policy – Website + Cookie Policy also apply to the marketing funnel for everyone (not motion-specific).
- ⚠️ = assumption to confirm.

## SaaS DPA & SLA ✅ resolved
- **SaaS DPA** — basis = the existing `1it8` ("Normain's provision of The Platform" — see Q1) → move into `02 - SaaS default agreements / 04 - DPA`, fill its subprocessor appendix. Separate lineage from the enterprise/Marketplace DPAs.
- **SaaS SLA** — create in `02 - … / 05 - SLA`, kept in place for when a tier needs it (separate from the enterprise SLA).
- **Confirm:** does the open SaaS have a paid tier that should carry an SLA, or is it purely free/experimentation (SLA stays dormant)?

## Q5 / O3 — subprocessor list content ✅ resolved 2026-06-04

**Working doc:** edits happen in `Pre-approved subprocessor list - 2026-06-04 - DRAFT`; the `2026-04-28 - LATEST` stays clean (Anthropic was removed from it). The DRAFT is the master superset; per-mode appendices (step 4) select from it.

| Vendor | Table | In which DPAs | Notes |
|---|---|---|---|
| **Anthropic** | 2 (operations) | **All** | Not Table 1 — AWS hosts the models, so AWS is the Table-1 entry. (Supersedes earlier "dedicated without Anthropic".) |
| **OpenAI** | 2 (operations) | **All** | Rename master's "ChatGPT" → "OpenAI" (legal entity). |
| **AWS** | 1 (hosting) | **SaaS only** | Excluded from enterprise DPAs. |
| **Sentry** | — | **None** | Stays in master as **pre-approved** (future use); not in any DPA. |
| **Firecrawl** | — | **None** | Excluded — no PII (customer-URL ingestion judged non-PII). |
| **Stripe** | — | **None (internal-tools list)** | Code-verified: only billing email + plan/org metadata go to Stripe; Normain = controller (its own billing), not processor of customer data → not a sub-processor. _(confirmed 2026-06-04)_ |
| **GitHub** | — | **None (internal-tools list)** | No customer PII — dev tool. |
| **Vanta** | — | **None (internal-tools list)** | No customer PII — compliance tool. |

Still open: Loops/Zero/Cursor + MS Marketplace publisher classification (Table 2 vs internal-tools).
