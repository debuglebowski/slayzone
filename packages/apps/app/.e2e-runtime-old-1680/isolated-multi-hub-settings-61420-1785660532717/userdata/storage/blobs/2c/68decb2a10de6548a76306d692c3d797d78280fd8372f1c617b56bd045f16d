# Renewal evidence — "Mobile code checked for vulnerabilities"

Vanta test `mobile-code-sec-tested` · ISO 27001:2022 · owner Kalle · **evidence expires Jul 2, 2026** (renew within ~2 weeks). Repo: `normainofficial/normain` (formerly `normain_dev`).

## What last year's 5 screenshots proved
1. GitHub Actions "Deploy Backend & Frontend" run — steps incl. Lint, Typecheck, Run tests, **Scan container for vulnerabilities**.
2. `package.json` scripts (root:ci / root:lint / root:typecheck / root:test).
3. `eslint.config` importing **eslint-plugin-security**.
4. eslint config enabling `securityPlugin.configs.recommended`.
5. Close-up of Lint + Typecheck CI steps passing.

## Important: the stack changed (evidence must be re-sourced, not 1:1)
- Linter migrated **ESLint + eslint-plugin-security → Biome** (`root:lint = biome check .`). The eslint-plugin-security screenshots (#3, #4) have **no direct equivalent** anymore.
- `root:typecheck` script removed (now tsgo / native preview).
- **New, stronger security scanning added** — and it directly answers the reviewer comment on the test ("could be achieved by adopting e.g. GitHub CodeQL" — now done):
  - **CodeQL** (SAST) — scans javascript-typescript source every PR + weekly.
  - **Trivy** — scans prod container images (api/worker/tenant-api) for CRITICAL/HIGH CVEs weekly.
  - **OWASP ZAP** — DAST baseline scan of app.normain.com + app-dev weekly.

## Recommended new evidence — capture genuine screenshots from these (logged in)

| # | Replaces | Capture (screenshot the GitHub UI) | URL |
|---|----------|-----------------------------------|-----|
| 1 | old #3/#4 (security static analysis) | **CodeQL run** — job "🛡️ Analyze javascript-typescript", all steps ✓ | https://github.com/normainofficial/normain/actions/runs/27698626452 |
| 2 | old #1 (container scan) | **Trivy run** — 3 image scan jobs ✓ (api/worker/tenant-api) | https://github.com/normainofficial/normain/actions/runs/27531751397 |
| 3 | old #1/#5 (lint+test) | **PR Checks run** — Lint (Biome) ✓ + Run tests (Vitest) ✓ | https://github.com/normainofficial/normain/actions/runs/27698626723 |
| 4 | old #2 (scripts) | **package.json** scripts block (biome check, vitest) | https://github.com/normainofficial/normain/blob/main/package.json |
| 5 | old #3/#4 (scanner config) | **codeql.yml** — javascript-typescript matrix + queries | https://github.com/normainofficial/normain/blob/main/.github/workflows/codeql.yml |

Strong extras (optional):
- **Security tab — code scanning alerts** (CodeQL + Trivy SARIF): https://github.com/normainofficial/normain/security/code-scanning
- **ZAP run** (DAST): https://github.com/normainofficial/normain/actions/runs/27521579125

All runs above verified **success** via `gh` on 2026-06-18.

## Note on capture
Screenshots must be genuine captures of the live GitHub UI. The agent browser here is logged into GitHub but can't capture images in this headless environment, and the screenshot-capable browser isn't logged into the private repo — so capture these on your own logged-in browser (each is a few seconds). Open URL → screenshot.
