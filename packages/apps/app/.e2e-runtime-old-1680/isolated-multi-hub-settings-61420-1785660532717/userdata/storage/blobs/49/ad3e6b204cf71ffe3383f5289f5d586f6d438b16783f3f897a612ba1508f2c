# Secure Configuration Baselines — 2026 Renewal Action Items

**Vanta test:** Secure configuration baselines developed (ISO 27001:2022 · A.8.9 CFG-44, A.8.27 SEA-56)
**Source:** Google Sheet "Secure Configuration Baselines" (111 rows / 34 systems · 85 Yes / 22 No / 4 N/A)
**Status:** Renewal due — current evidence expires Jul 14, 2026. Last edited 2025-07-30.

## Bottom line
Doc is sound; no rebuild. Clearing the Vanta test = re-upload non-expired file. Passing the ISO audit *cleanly* = address only the rows below. The other 15 "No" rows are defensible plan-limitations → no work.

**Verified live against the actual services** (codebase @ `normainofficial/normain` main · GitHub API · Azure CLI/ARM · Microsoft Graph as Global Admin): #4 already effectively solved, #5 mostly built. #1, #2, #7 confirmed open (some worse than the doc states). #3 needs Purview portal; #6 is a process call.

**Legend:** 🟢 solved (verified) · 🟡 mostly there / one small change · ❌ confirmed open · ❓ not reachable via CLI, needs portal

## Action items

| # | System · Config | Baseline | 🔎 Verified | Finding (live evidence) | Minimal action | Owner |
|---|---|---|---|---|---|---|
| 4 | Normain SaaS App · strong-password + MFA | No | 🟢 | Prod auth is **SSO-only**: `PASSWORD_LOGIN_ENABLED` defaults `false`, not set in `deploy-k8s-prod.yml` → no in-app password login; MFA enforced at IdP (Google/MS). No 2FA plugin needed | Reword row → "Yes — SSO-only, MFA at IdP" | Kalle |
| 5 | Managed PostgreSQL · query/access logs | No "Todo" | 🟡 | Pipeline **done** (`audit.tf`): `PostgreSQLLogs` → Log Analytics `law-…-audit` + immutable storage w/ retention. Gap: pgaudit loaded but `pgaudit.log = NONE` → statement audit off | One-line TF: set `pgaudit.log = WRITE,DDL,ROLE` (or risk-accept conn-logs only) | Kalle |
| 1 | GitHub · Secret scanning alerts | No | ❌ | API: `secret_scanning = disabled` (push-protection + dependabot also off). NB: org-level **2FA enforced = true** | Enable (free) → flip to Yes | Gustav |
| 2 | Microsoft 365 · Conditional access | No | ❌ | Graph (as Global Admin): **0 Conditional Access policies** in tenant. MFA still enforced by other means (M365/Azure MFA = Yes in doc). Security-Defaults state not CLI-readable (token scope) | Implement basic CA (needs Entra P1) OR risk-accept + reword to cite MFA-via-other-means | Dennis |
| 7 | Microsoft Azure · Security Center recs | No | ❌ | ARM: Defender **on** (CloudPosture + Containers = Standard) but **4,914 unhealthy recommendations**, secure score **15.5%** (3.88/25). Far past "some remaining" | Triage top recs OR document remediation plan + risk-accept interim | Kalle |
| 3 | Microsoft 365 · DLP | No | ❓ | Not exposed via Graph/az (Purview-only). Needs Purview portal or Security&Compliance PowerShell | Portal check + add justification | Dennis |
| 6 | GitHub · org audit-log review | No | ❌ | Org audit log exists (GitHub-retained) but no review cadence; log-streaming not configured/visible. Process item, not code-fixable | Define light quarterly review OR risk-accept | Gustav |

## Notable side-finding (beyond this test)
Azure **secure score 15.5% with 4,914 unhealthy Defender recommendations** is low and likely material to other ISO/SOC2 controls (vuln mgmt, hardening). Worth a separate triage task — not all are in-scope here, but the headline number will draw auditor attention.

## Already-fine "No"/"N/A" rows (no action)
1Password (req. 1P Business), ChatGPT usage logs (Enterprise only), iOS passcode/wipe/app-block (no MDM → policy + offboarding), Linear/Miro (Enterprise), Slack file sharing (business need), Superhuman MDM (Enterprise), Google Workspace external sharing (business need, monitored) & DLP (req. GW Enterprise), OpenAI API logs (prod via Azure). N/A: ChatGPT & Cloudflare API keys, Linear & Slack audit logs.

## Sequence to renew
1. Re-review + re-date doc for 2026 (~1 hr desk).
2. Close items: #4 reword (already true) · #5 one-line TF · #1 free toggle · #2/#3 decide CA/DLP (risk-accept fastest) · #6 process call · #7 needs a remediation-or-accept decision (biggest item).
3. Re-export sheet → PDF → upload to Vanta → re-attest.

**Effort to renew: ~half a day** if #2/#3/#7 are risk-accepted with justification; longer if you actually remediate Defender recs or stand up Conditional Access.
