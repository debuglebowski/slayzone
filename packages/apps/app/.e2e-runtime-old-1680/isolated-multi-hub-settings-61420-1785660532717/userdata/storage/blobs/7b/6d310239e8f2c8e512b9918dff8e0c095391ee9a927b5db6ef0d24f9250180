# Slice 3/8 — file-editor IPC → tRPC router (P11)

**Commit:** `79897803` — `feat(transport): file-editor router (P11)` — direct to `main`, no PR.

## Done
- 15 `fs:*` IPC handlers mirrored as tRPC `fileEditorRouter`: 12 query/mutation
  + `showInFinder` (dynamic electron import) + `watch` subscription
  (`observable<FileWatchEvent>`). watch+unwatch+fs:changed+fs:deleted → one stream.
- Registered `fileEditor` in `appRouter`. Added transport → `@slayzone/file-editor` dep + lockfile.

## Branch divergence handled
Ref `edfc8c99` (unmerged `phase-0-path-port-hardening`) assumed an electron-free
`@slayzone/file-editor/server` store. On `main` it did NOT exist — all logic was inline
in `src/main/handlers.ts`. So this slice folds in file-editor's domain-split (structurally
required by the router):
- NEW `src/server/file-ops.ts` (pure fs ops) + `src/server/watcher.ts` (single-callback
  subscribe) — electron-free.
- `src/main/handlers.ts` rewritten to **thin Electron glue** over the store (IPC watch
  bridges `subscribeFileWatcher` → `webContents.send`). **Single source of truth** per
  migration intent; IPC + tRPC now share one impl.

## Scope preserved
- No renderer cutover, no IPC handler deletion (slice 5).
- `server/**` stays electron-free — `lint:server-boundary` guard passes.
- Concurrent sibling slices in the tree (projects, task-terminals, integrations) left
  uncommitted/untouched; committed only my 9 files via partial `git apply --cached`.

## Verification
- `pnpm typecheck` — GREEN (all pkgs incl file-editor, transport, app, server).
- `pnpm build` (prod) — GREEN (transport→file-editor bundles; cross-pkg resolution OK).
- e2e `core/44-file-editor` + `core/45-file-tree-rename` — **34/34 PASS**.
- file-editor contract test — 18/19 (the 1 ✗ `readDir filters gitignored` is
  PRE-EXISTING & stale: `readDir` marks `ignored:true` but includes entries; byte-identical
  to HEAD behavior — not a regression).
- lint: my files **0 issues**; theme + server-boundary guards pass. (eslint repo-wide
  302>301 max-warnings is pre-existing, from concurrent client-side slices — none mine.)

## Files (9)
- `packages/domains/file-editor/src/server/{file-ops,watcher,index}.ts` (new)
- `packages/domains/file-editor/src/main/handlers.ts` (fat→thin)
- `packages/domains/file-editor/package.json` (`./server` export)
- `packages/shared/transport/src/server/routers/file-editor.ts` (new)
- `packages/shared/transport/src/server/router.ts` (register)
- `packages/shared/transport/package.json` + `pnpm-lock.yaml` (dep)
