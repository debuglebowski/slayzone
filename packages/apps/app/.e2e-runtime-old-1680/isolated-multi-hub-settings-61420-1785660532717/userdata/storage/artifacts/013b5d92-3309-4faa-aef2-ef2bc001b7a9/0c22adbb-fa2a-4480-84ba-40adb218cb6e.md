# Vanta SOC2 — "Remote access MFA enforced" (IAC-12) — research

**Control:** Remote access MFA enforced — IAC-12 (Vanta source), Domain: Identification & Authentication
**Requirement:** Production systems only remotely accessible by authorized employees with a valid MFA method.
**Mapped to:** SOC2 CC 6.1 + CC 6.6
**Status:** Needs evidence — **0/0 evidence**
**Owner:** Kalle Hansson

---

## (a) What Vanta requires
A Vanta control passes when it has ≥1 piece of **passing evidence mapped to it** (automated test, document, and/or policy). This control currently has:
- Tests: **none associated**
- Documents: **none associated**
- Policies: **none associated**

So it is not failing on substance — it is empty. To pass it needs MFA-enforcement evidence for remote access to production mapped to IAC-12: the relevant passing automated MFA tests + the Access Control Policy that mandates MFA for remote/privileged access.

## (b) What already exists (real, passing)
**In Vanta (passing, but NOT mapped to this control):**
- ✅ `MFA on GitHub` — automated, OK (SOC2, ISO 27001)
- ✅ `MFA on Google Workspace` — automated, OK (SOC2, ISO 27001)
- ✅ `The company requires authentication to production datastores to use authorized secure authentication mechanisms, such as MFA` — document test, OK (SOC2)

**In Drive (authoritative — 01 - Official InfoSec Policies):**
- `[Normain] Access Control Policy.docx` (id `1wQZAmCnydHhOWiQOmI3z6Zj8avLn5RSZ`) contains:
  - "All privileged access to company production infrastructure shall use Multi-Factor Authentication (MFA) when available."
  - "Remote connections to company production systems and networks must be encrypted"
  - "Enforce Strong Authentication: Require MFA for all privileged access when available."

## (c) The gap
Purely a **mapping gap**, not a controls gap. Passing MFA evidence + an approved policy already exist; none of it is attached to IAC-12, so Vanta reads 0/0. No new evidence needs to be produced.

## (d) Concrete steps to close it
1. Open control IAC-12 in Vanta → map existing passing tests: **MFA on GitHub**, **MFA on Google Workspace** (+ any AWS/Azure/IdP MFA test that gates production access, if present).
2. Map the **Access Control Policy** document (or the passing MFA document test) to IAC-12.
3. Confirm mapped items show passing → control flips Needs evidence → passing (CC 6.1 & CC 6.6 both satisfied via the shared mapping).
4. Auditor-completeness check: confirm every remote path into production (GitHub/IaC, Google SSO, cloud consoles) is MFA-gated; add a cloud/IdP MFA test mapping if such a test exists.

*Note: only existing, real evidence is recommended for mapping — nothing fabricated.*
