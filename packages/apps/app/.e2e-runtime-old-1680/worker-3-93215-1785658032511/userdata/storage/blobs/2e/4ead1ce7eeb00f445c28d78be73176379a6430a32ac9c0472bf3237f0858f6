# Subprocessor List — Situation & Remediation Plan

## Situation
- Authoritative list lives in **DPA Appendix B**, with TWO tables:
  - **Table 1 — platform PII subprocessors**: Microsoft Azure, BetterStack, PostHog
  - **Table 2 — company / support / marketing**: 1Password, Circleback, Anthropic, Linear, Google Workspace, Slack, Attio, … (the "whole company" list)
- List "last updated 25 February 2026" — **not fully current**. Known gaps:
  - **Firecrawl** — used in the data path (customer-URL ingestion) but NOT listed.
  - **Anthropic** — listed (Table 2) but currently dev/non-production only; production move planned post-period.
  - **OpenAI** — not listed; used in dev only.
  - Possibly others (full re-sweep needed).
- Obligation: DPA §8.1 / Microsoft Standard Contract §2.3.d — **14-day prior notice** to customers before any subprocessor change, with objection + termination right.
- When the list is corrected, each active customer must receive notice **with a diff vs the version they signed**.

## Track A (PRIMARY) — Finish the System Description
- Finalize `revised` → Google Doc → Sara review → Prescient.
- Description references the **register of record** (DPA Appendix B + Vendor Master Sheet), it does NOT enumerate exhaustively → Track A is **not blocked** by Track B.

## Track B (SECONDARY) — Subprocessor list remediation (follows own policies)
- **B1 Reconcile**: actual subprocessor set vs Appendix B. Resolve Firecrawl (PII subprocessor or not?), OpenAI/Anthropic treatment, any missing vendors. Decide the two-table structure + naming (platform vs company-wide).
- **B2 Update Appendix B**: corrected lists + new "last updated" date; update Vendor Master Sheet + Vanta to match.
- **B3 Diff per customer**: for each active customer, diff the new list vs the version they signed.
- **B4 Notify customers**: send 14-day notice per DPA §8.1 with the diff. (Customer comms — requires explicit authorization before sending.)

## Ordering
- A finishes independently.
- B should complete **before issuance** so the register the auditor samples is current.
- B4 (customer notice) is a standalone policy/contractual obligation, independent of the audit.

## Open decisions
- Firecrawl: PII subprocessor → add to Appendix B, or confirm no PII?
- OpenAI: add now (planned) or only on activation?
- Two-list naming (application vs company-wide).
- ISML / DPO documentation conflict (CEO-in-practice vs CTO-in-docs) — see system-description CC1.
