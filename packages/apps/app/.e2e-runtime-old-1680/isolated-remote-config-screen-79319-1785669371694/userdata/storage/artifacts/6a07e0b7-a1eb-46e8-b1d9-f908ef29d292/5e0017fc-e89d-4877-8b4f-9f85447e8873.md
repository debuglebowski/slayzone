# Handoff instructions — SlayZone Chromium migration

Short step-by-step for the human to execute the orchestrator handoff. Companion to `ORCHESTRATOR-HANDOFF.md` (the full retrospective + state + plan).

---

## 1. Close this session

Stop interacting with the current Claude Code session. Start a new one in the same working directory:

```
/Users/Kalle/dev/projects/slayzone-chromium
```

## 2. Paste this opening prompt in the new session

```
You are the new orchestrator for the SlayZone Chromium migration. The previous orchestrator drifted and the user has lost confidence. Your first action is to read docs/chromium/ORCHESTRATOR-HANDOFF.md in full — not skim, in full — before touching anything else.

After reading, confirm back to me:
1. The three visual gaps you see when you launch the fork vs Electron side-by-side
2. Which item in the iteration backlog you will start with
3. Your commitment to the non-negotiable protocol (validate composition against Electron every phase close, maintain DEFERRED.md, cap parallel agents at 2)

Parent task: 6a07e0b7-a1eb-46e8-b1d9-f908ef29d292
Load the slay skill before any slay CLI commands.
```

## 3. What the new session will do automatically

- Read `docs/chromium/ORCHESTRATOR-HANDOFF.md` (the 20KB handoff doc, commit `617a6abd`)
- Read `docs/chromium/layout-modes.md` + `docs/chromium/iteration-data-wire.md` for recent iteration state
- Run `git log --oneline -30` for latest commits
- Launch both Electron and fork + take side-by-side screenshots
- Create `DEFERRED.md` at repo root, populated from the handoff's Tier 1/2/3 gap lists
- Start `iter-writes` subtask as the first real work (end-to-end CRUD for tasks + projects — the single biggest unblock)

## 4. Key reference locations

**In git (canonical, always readable)**:
- `docs/chromium/ORCHESTRATOR-HANDOFF.md` — full retrospective + state + plan
- `docs/chromium/HANDOFF-INSTRUCTIONS.md` — this file
- `docs/chromium/layout-modes.md` — layout-mode fidelity notes + screenshots
- `docs/chromium/iteration-data-wire.md` — data-wire diagnosis + seed plan
- `docs/chromium/phase-8-smoke.md` — Phase 8 smoke notes
- `docs/chromium/phase-9-smoke.md` — Phase 9 smoke notes

**Slay assets on parent task `6a07e0b7-a1eb-46e8-b1d9-f908ef29d292`**:
- Root level: handoff copies (`b3363743` / `377ab825`) — duplicates of the git version
- `part-1-archive/`: all older Part I planning docs (execution-plan, orchestrator-brief, session-bootstrap, holy-shit-path, orchestrator-kickoff, migration-investigation, chromium-fork-implementation)

## 5. Red flags — watch the new orchestrator

Interrupt and point them back to Section 4 of `ORCHESTRATOR-HANDOFF.md` if you see:

- Closes a subtask without producing a screenshot
- Runs 3+ agents in parallel
- Accepts agent reports without verifying
- Claims "done" on a phase where `DEFERRED.md` still has items in that phase's scope
- Skips the Electron vs fork diff step

## 6. Launch commands (paste to the new session if asked)

**Launch fork with seed data + home mode**:
```bash
SLAYZONE_SEED_DEMO=1 ./scripts/chromium/run.sh -- \
  --slayzone-layout-shell \
  --slayzone-layout-mode=home \
  --slayzone-webui-bundle-dir=$(pwd)/packages/webui \
  --user-data-dir=$(mktemp -d)
```

**Launch Electron (reference baseline)**:
```bash
pnpm --filter @slayzone/app dev
```
If the exact command has drifted, check `packages/apps/app/package.json`.

**Compare harness**:
```bash
./scripts/chromium/compare-with-electron.sh
```

## 7. First outcome you should expect

Within the first hour of the new session:
- It has read the handoff and confirms the three visual gaps
- It has launched both apps and screenshotted them
- It has created `DEFERRED.md`
- It has started the `iter-writes` subtask and the agent is planning

If an hour passes without those, ask the new orchestrator "what have you validated so far?" That question is the tripwire.
