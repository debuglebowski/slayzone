Root cause: The PR-approval requirement on `normainofficial/normain` was introduced by the `Main branch rules` ruleset (id 13779853) created on 2026-03-11. The 47 flagged PRs were merged before that date during a 2-person engineering team era when no formal approval requirement existed; PRs were self-merged with synchronous review (pair-programming, in-person/Slack discussion) rather than recorded GitHub approvals.

Remediation in place (2026-03-11 → 2026-04-28):
1. Active ruleset on `main` requires 1 approving review (other than author), CODEOWNERS review, conversation resolution before merging, linear history, and squash merges. Source: https://github.com/normainofficial/normain/rules/13779853
2. Bypass actors hardened on 2026-04-28: removed direct user bypasses (PhilipNgo, redsuperbat); admin role downgraded to `pull_request` mode (no direct push); only `normain-ci-bot` retains `always` bypass for the automated `tenants.json` commit in `provision-tenants.yml` (scoped GitHub App, contents:write only).
3. CODEOWNERS added (PR #1977) listing all 3 current developers; ruleset enforces code-owner review.
4. All other org repos (21x) archived 2026-04-28 — only `normain` is in scope.

Compensating evidence: All 47 PRs were authored by current or former org admins working on a 2-person team operating under synchronous review (pair-programming and in-person discussion). No security or production incidents have been traced to these PRs. Post-ruleset (2026-03-11 → present): 0 self-merged unreviewed PRs — the formal control is verified working.

Linear change-management ticket: NOR-2594.

Deactivating these 47 historical PRs indefinitely: they are already merged and cannot retroactively receive recorded approvals; future PRs are covered by the ruleset above.
