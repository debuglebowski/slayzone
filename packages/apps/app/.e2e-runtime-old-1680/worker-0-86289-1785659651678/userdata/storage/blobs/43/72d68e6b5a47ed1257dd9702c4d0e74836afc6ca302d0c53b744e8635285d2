flowchart LR
    User["User"]
    Admin["Normain<br/>tech admin"]

    subgraph Azure["Microsoft Azure"]
        subgraph Sub["Subscription"]
            subgraph RG["Resource Group"]
                AI["AI Services"]
                Storage["Storage<br/>+ Key Vault"]

                subgraph VNet["Virtual Network"]
                    subgraph AKS["AKS cluster"]
                        GW["Ingress Gateway<br/>app.normain.com"]
                        Tenant["Tenant workloads<br/>API + Worker + Redis"]
                    end
                    PG["Postgres<br/>vector embeddings"]
                end
            end
        end
    end

    User --> GW --> Tenant
    Tenant --> PG
    Tenant --> AI
    Tenant --> Storage
    Admin -. Entra PIM .-> AKS
    Admin -. Entra PIM .-> PG
