# Breakdown — Layout A: horizontal bars

Labels left, bars + counts inline. Reads top-to-bottom like a ranked
list; best default for enum/user groupBy with <10 groups.

```
+------------------------------------------+
| Deals by stage                    [bar]  |
+------------------------------------------+
| open      ######################## 64    |
| nego      ##############     38          |
| won       #########     26               |
| lost      ####     11                    |
| (none)    ##     4                       |
+------------------------------------------+
| 143 instances . groupBy: stage           |
+------------------------------------------+
```

Config: groupBy field, chart="bar", conditions pre-filter.
Bar click -> filtered list drill (future).
