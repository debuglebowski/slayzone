# Lint: Safe-to-Fix Triage

_Generated by 8 parallel read-only sub-agents auditing every flagged site (~300). Verdicts are
**pattern-based** — the dogfood tree churns, so exact line numbers drift; re-run eslint before fixing._

## ✅ BOTH safe wins DONE (commit `c3023714`) — 42 warnings cleared, 0 errors, typecheck green

### 1. static-components → deleted dead file (11 ✅)
`packages/domains/ai-config/src/client/LayoutPreviews.tsx` deleted (orphaned, verified unused).

### 2. refs "latest-value mirror" relocation (31 ✅)
`xRef.current = x` moved from render into `useEffect` across editor, file-editor, task-browser,
task-terminals, terminal, shared/ui, worktrees. 1 correctly SKIPPED: `git-diff-store.ts` `refreshRef`
(load-bearing stable identity for `useImperativeHandle`).

## Current totals (after sweep — 226 warnings)
| Rule | Count | Completely SAFE remaining | Needs care | Unsafe / intentional |
|---|--:|--:|--:|--:|
| set-state-in-effect | 156 | ~2 | ~58 | ~96 |
| refs | 52 | ~0 (the easy mirrors are done) | ~25 | ~27 |
| immutability | 11 | ~3 (test rename) | ~8 | 0 |
| purity | 5 | 0 | 5 | 0 |
| preserve-manual-memoization | 2 | 0 | 0 | 2 (load-bearing) |
| **TOTAL** | **226** | **~5** | ~96 | ~125 |

## ⛔ What's left is care-needed — do NOT bulk-fix
- **set-state-in-effect (~154):** async IPC fetches, event/IPC subscriptions, intentional auto-nav,
  form-draft resets from props, DOM measurement, debounce timers, exit-animation lifecycles. Per-site
  redesign or justified `eslint-disable`. The big bucket is the risky one.
- **refs (~52 remaining):** render-time READS feeding JSX (rich-text-editor ArtifactPicker gate,
  TerminalSplitGroup xterm/chat handle refs), rAF / WebContentsView bounds-sync, `useSyncExternalStore`
  stable identity, lazy-init singletons (`use-undo`), DOM-measure fit math.
- **purity (5):** relative-time `Date.now()` (pulling out of render stales the displayed age),
  `Math.random()` skeleton width in `useMemo([])` (load-bearing stable width). Need a tick/lazy-init.
- **immutability (~8):** used-before-declare reorders, window-store installs, scroll DOM writes.
- **preserve-manual-memoization (2):** git-diff-store `useSyncExternalStore` — intentional, leave.

## Recommendation for remaining work
Tackle per-rule via the subtasks, NOT as mechanical sweeps. Each needs reading the site +
verification. The cheap, zero-risk wins are already taken.
