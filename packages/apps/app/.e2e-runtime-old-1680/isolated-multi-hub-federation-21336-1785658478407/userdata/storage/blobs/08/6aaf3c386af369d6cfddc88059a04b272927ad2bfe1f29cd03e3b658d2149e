flowchart TD
    subgraph H["Hook & mental model"]
        H1["From calculators to 'thinking' machines"]
        H2["Einstein-in-your-basement metaphor"]
        H3["Capabilities vs. limits — prompt engineering as the key skill"]
        H1 --> H2 --> H3
    end

    subgraph M["Terminology & mechanics"]
        M1["AI vs. generative AI vs. LLM vs. GPT/Transformer"]
        M2["Neural network = parameters; next-word prediction"]
        M3["Training: pretraining (guess-the-next-word + backprop) + RLHF"]
        M4["Frozen model + later fine-tuning"]
        M1 --> M2 --> M3 --> M4
    end

    subgraph L["The model landscape"]
        L1["Variety: open/closed, free/paid, local/cloud, quality tradeoffs"]
        L2["Modalities: text-to-text, text-to-image, image-to-text,<br/>speech-to-text, text-to-audio, text-to-video, multimodal"]
        L1 --> L2
    end

    subgraph E["Emergent capabilities & real use"]
        E1["Emergent abilities: roleplay, poetry, code, advice"]
        E2["Reasoning example (scissors / egg / fire)"]
        E3["Personal use cases: coding, walk-and-talk, photo analysis"]
        E1 --> E2 --> E3
    end

    subgraph I["Implications & mindset"]
        I1["Human vs. AI capability trajectory"]
        I2["Pace of this revolution vs. past ones"]
        I3["Denial vs. panic vs. balanced mindset"]
        I4["Is role X still needed? — human judgment, AI as colleague, human+AI combo"]
        I1 --> I2 --> I3 --> I4
    end

    subgraph P["Practical application & future"]
        P1["Model vs. product distinction, APIs"]
        P2["Prompt engineering techniques: context, interview method, iteration"]
        P3["Future: autonomous agents with tools"]
        P4["Wrap-up / key takeaways"]
        P1 --> P2 --> P3 --> P4
    end

    H --> M --> L --> E --> I --> P
