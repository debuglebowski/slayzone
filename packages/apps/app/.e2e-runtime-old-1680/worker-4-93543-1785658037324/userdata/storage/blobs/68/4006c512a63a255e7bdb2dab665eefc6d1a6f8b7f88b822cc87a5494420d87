# Web team hand-off — Website Cookie Policy

**Goal:** make `normain.com/cookie-policy` current, canonical, and linked. Today it serves an old/legacy copy, is unlinked, and its vendor list is partly inaccurate.

## 1. Publish from the canonical doc
The live page is served from a stale/legacy source. Re-point it to the canonical Drive doc:

- **Doc:** `Cookie Policy - 2024-08-05 - LATEST`
- **Path:** Normain (shared drive) / `05 - InfoSec` / `02 - Official Other Policies` / `12. Website - Cookie Policy`

Steps: Google Docs → **File ▸ Share ▸ Publish to web** on that doc, then point the `normain.com/cookie-policy` redirect at the new `…/pub` URL — same pattern already used for `/privacy-policy` and `/terms` (both publish straight from their canonical docs and auto-sync).

## 2. Add a footer link
The website footer lists **Privacy** and **Terms** but **not Cookies**. Add a **"Cookies"** link → `/cookie-policy`.

## 3. Reconcile the vendor list (content is out of date)
Live trackers actually firing on `normain.com` (verified 2026-06-08, homepage load):

| Vendor | In policy? | Observed live? | Action |
|---|---|---|---|
| Google (Analytics + Tag Manager) | ✅ | ✅ `G-88XLPTCN0F` / `GTM-PWQ44RKQ` | keep |
| Framer (site platform) | ❌ | ✅ `events.framer.com` | **add** |
| HubSpot | ✅ | ❌ not seen | verify (GTM tag / forms?) or remove |
| LinkedIn | ✅ | ❌ not seen | verify or remove |
| Reddit | ✅ | ❌ not seen | verify or remove |
| Salesloft | ✅ | ❌ not seen | verify or remove |
| Intercom | ✅ | ❌ not seen | verify or remove |

Note: GTM can load tags conditionally (consent, specific pages, form interactions), so the 5 "not seen" vendors may still fire elsewhere — confirm in GTM before removing.

## 4. Versioning caveat
Publish-to-web is bound to a specific file. When a new cookie-policy version is cut (the convention copies forward to a **new** dated file), re-publish the new file and update the redirect — otherwise the live page keeps serving the old version.

## Out of scope here (separate decisions)
- No cookie **consent banner** exists on the site despite analytics/marketing trackers — likely a consent-compliance gap; raise with legal.
- The **app** (`app.normain.com`) cookie disclosure is being folded into the App Privacy Policy (no separate app cookie page) — not part of this hand-off.
