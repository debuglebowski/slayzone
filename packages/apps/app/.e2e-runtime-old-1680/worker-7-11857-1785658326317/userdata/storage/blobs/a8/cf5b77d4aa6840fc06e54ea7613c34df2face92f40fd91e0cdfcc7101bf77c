# Open Decisions — SLA / DPA / Subprocessor consolidation

_Companion to the structure plan (`Drive structure proposal`). Tracks the content/legal decisions, not the folder layout. As of 2026-06-03._

**Status:** 🟢 recommendation ready (needs your nod) · 🟡 needs your input · ⏸ deferred

## Document relationships (reference — settles "is the SLA part of the DPA?")

| Doc | Role | Notes |
|---|---|---|
| Main Agreement | Umbrella contract for direct/enterprise deals | |
| Attachment 1 — Terms of Service | Commercial/service terms (enterprise) | |
| **Attachment 2 — SLA** | Uptime commitment | **Separate document** |
| **Attachment 3 — DPA** | Data-processing terms (GDPR Art. 28) | **Separate document; the subprocessor list is embedded *inside* it as an appendix** |
| Terms of Use (Product) + Privacy + Cookie | Self-serve SaaS click-through terms | |

→ **SLA is NOT part of the DPA** (sibling attachments). **Subprocessor list IS part of the DPA.** A whole-package snapshot holds the SLA and the DPA side by side.

## Q4 — SLA versions 🟢
**Finding:** ~23 "Attachment 2 – SLA" docs exist (the "25+ versions") — **mostly per-customer copies**, not distinct template versions. Distinct *content* versions: **90%** (older) → **99%** (KPMG final, 2025-12-17) → **2026-05-29 draft** (99%, cleanup to match the SOC 2 system description).
**Recommendation:**
- Don't recreate 23 snapshots. Per-customer copies stay frozen in their customer folders.
- Template motion folder carries: **99% as `- LATEST`** + **2026-05-29 as `- DRAFT`**; optionally the **last 90% as one bare-date historical**; older scattered copies → `99 - Old`.
- The proposed `2025-12-27 / 2025-12-01` ≈ the 99% rollout; the real anchor is **2025-12-17**.
**Needs:** keep a 90% historical template snapshot, or just archive it?

## Q3b — Cookie Policy: product vs website? 🟡
**Finding:** ONE Cookie Policy (last updated Aug 2024). It claims to cover "our website **and** platform," but every third-party cookie listed is website/marketing — **Google, HubSpot, Intercom**.
**Recommendation:** split only if the **product app sets materially different cookies** than the website. Likely the website carries analytics/advertising (HubSpot/Intercom) while the logged-in product uses only necessary/session cookies — if so, a separate **Cookie Policy – Product** is more accurate and consistent with the Privacy/Terms Product-vs-Website split.
**Needs:** does the app set any non-essential cookies/trackers? (product/eng input)

## Q1 — Hosting DPA = two models, not three copies 🟡
The 3 "hosting DPA" files are **two different contracts**, split by **who runs the servers**:

| Model | Docs | Subprocessor clause | List incl. Azure? |
|---|---|---|---|
| **Normain-hosted** (Normain's Azure) | `11xa`, `1it8` (newest) | list-controlled — "shall not use subprocessors other than…" | Yes |
| **Customer-hosted** (customer's own cloud) | `14JrU` | looser — "general consent, list on request" | No (it's the customer's infra) |

`11xa` + `1it8` are two copies of the **same** (Normain-hosted) contract → keep the newest (`1it8`), populate its empty appendix. `14JrU` is the **other** contract (customer-hosted) → keep separately.
**Needs:** does `02 - Customer Cloud Hosting` sell **Normain-hosted** (the Advisense-style direct deal), **customer-self-hosted** (`14JrU`), or **both** (→ two motion folders)? The folder name says customer-hosted, but your direct deals were Normain-hosted.

## Q3 — Terms of Use vs DPA — what's praxis 🟢
- **Self-serve SaaS:** customer click-accepts **Terms of Use (Product)** + Privacy + Cookie. Terms of Use *is* the contract.
- **Enterprise/negotiated** (Marketplace, hosting): a **negotiated Main Agreement** (or Marketplace Order + Microsoft Standard Contract) is the contract, with its own commercial terms (your **Attachment 1 – Terms of Service**). The click-through Terms of Use does **not** apply — the negotiated agreement replaces it.
- **The DPA does NOT fill the Terms role.** It's a separate **data-protection** layer (GDPR Art. 28) that sits *under* whichever contract applies. Terms/Main Agreement = the commercial+service deal; DPA = how personal data is processed.

→ So Terms of Use is the self-serve contract; enterprise uses the Main Agreement (+ Attachment 1); the DPA is an annex in both, never a substitute for Terms.

## Q5 — what actually goes inside the subprocessor lists ⏸ (deferred from the mode-mapping)
Parked content sub-items (from the parent task):
- **Firecrawl** (Mendable AI) — in the data path (customer-URL ingestion); PII subprocessor → add, or confirm no PII?
- **OpenAI** — add now (planned) or only on activation?
- **Anthropic** — per-mode: SaaS list *with*, dedicated list *without*.
- **Table 2 completeness** — Stripe, Loops, Zero, Cursor, GitHub, Vanta, MS Marketplace publisher: which belong on the customer-facing register vs internal-tools list?
- **Mode↔list mapping** (original Q5): Marketplace/privacy-first = dedicated, *without* Anthropic; SaaS/experimentation = open multi-org, *with* Anthropic — confirm, incl. whether "Marketplace" (channel) = "dedicated tenant" (deployment) 1:1.
