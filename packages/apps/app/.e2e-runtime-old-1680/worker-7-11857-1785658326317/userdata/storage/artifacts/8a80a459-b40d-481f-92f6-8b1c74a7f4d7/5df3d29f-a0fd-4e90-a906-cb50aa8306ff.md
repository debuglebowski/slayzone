# Slice 3/8 — agent-turns tRPC router (P6, first streaming subscription) ✅

**Commit (direct main, no PR):** `63ca2c5a` — `feat(transport): agent-turns router (P6 — first streaming subscription)`

## What landed
1 IPC handler (`agent-turns:list`) + 1 broadcast (`agent-turns:changed`) mirrored to tRPC.
First streaming subscription in the migration.

- `agentTurns.list` — query wrapping extracted `listAgentTurnsForWorktree`.
- `agentTurns.onChanged` — `publicProcedure.subscription(observable<string>(...))` wrapping the
  domain emitter. Registered in `appRouter` (`{ agentTurns, tags }`).

## ⭐ TypedEmitter location (REUSE THIS in chat/pty/menus/notify slices)
- **Generic helper:** `TypedEmitter<TEventMap>` at `packages/shared/platform/src/typed-emitter.ts`,
  exported as subpath **`@slayzone/platform/events`** (node:events under the hood).
  - NOT in platform's main barrel — that barrel is imported by renderer code; node:events there
    would poison the web bundle. Always import the emitter helper via the `/events` subpath.
- **Per-domain instance pattern:** `packages/domains/agent-turns/src/main/events.ts`
  → `export const agentTurnsEvents = new TypedEmitter<AgentTurnsEventMap>()`.
- **Router wiring pattern:** `transport/src/server/routers/agent-turns.ts` `onChanged`:
  `observable<T>((emit) => { evts.on(k, h); return () => evts.off(k, h) })`.
- **Emit site:** `agent-turns/src/main/turn-tracker.ts` `broadcastChange()` emits on the bus
  before the legacy webContents.send block.

## Coexistence (slice 5 deletes IPC side)
Dual-emit during migration: IPC handler kept + `webContents.send` kept (renderer still on IPC) +
emitter added. No renderer/preload/types changes this slice.

## Adaptations vs ref commit 7eef7fb4
Ref is on divergent branch `phase-0-path-port-hardening` (NOT in main), older timeline:
`src/electron/`+`src/server/`, emitter pre-existed, sync DB.
Current main: `src/main/` only, NO emitter (created here), **async SlayzoneDb** (worker thread,
85fc667d) → `listAgentTurnsForWorktree` is async/awaits the bridge.

## Verification
- `pnpm typecheck` — ✅ whole monorepo green
- `pnpm lint` — ✅ my 4 new/edited files: 0 problems. (1 repo error is pre-existing
  `no-restricted-syntax`/setInterval in terminal/useTerminalStateStore.ts:242 — unmodified, not mine.)
- agent-turns `handlers.test.ts` — ✅ 15/15 (extraction behavior-preserving)
- `pnpm build` — ✅ prod build green (new router + agent-turns/main emitter + platform/events all bundle)
- e2e smoke (`01-smoke.spec.ts`) — ✅ 3/3 (app boots with changes)

## Notes / caveats
- e2e suite (129 specs) has ZERO agent-turns coverage and renderer is still on IPC this slice →
  full suite gives no signal on this change; ran smoke as boot sanity instead.
- `turn-tracker.test.ts` FAILS, but it is **pre-existing breakage** from the async-SlayzoneDb move
  (85fc667d): it uses un-awaited sync `db.prepare().get()/.all()` → "got not array". My change is
  the additive `agentTurnsEvents.emit` line only; not the cause. Worth a separate test-migration fix.
- Committed ONLY my 11 files; unrelated in-flight work (task/tasks domain refactor, SKILL.md edits)
  left unstaged.
