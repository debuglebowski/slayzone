# MON-4 — Infrastructure performance monitored — Investigation

**Control:** SOC 2 CC 7.2 + A 1.1 + PI 1.3 (3 frameworks)
**Vanta controlId:** `system-threshold-monitoring`
**URL:** https://app.eu.vanta.com/c/normain.com/controls?search=MON-4&controlId=system-threshold-monitoring
**Owner:** Kalle Hansson
**Investigated:** 2026-04-30

---

## Overview — TL;DR

MON-4 is **already passing 9/9 active tests** in Vanta. The task brief said 7/9 with 2 failing — that is stale. The 2 tests it referenced (`Virtual machine scale set CPU monitored` + `Storage Accounts health monitored`) are the **same 2 failing tests as CAP-7** (`97fb6cf5`), and Max already applied the CAP-7 Fix 1 + Fix 2 (VMSS CPU alerts + storage availability alerts on all 10 flagged accounts). Vanta's tests are all green. **No new remediation needed for MON-4.** Audit-ready as-is.

## Situation

### Vanta state (2026-04-30)
- Drawer header: `9 / 9 OK` · `fa-circle-check fa-solid`
- 1 test/document deactivated (informational; not audit-blocking)
- No documents or policies associated with MON-4
- Frameworks: SOC 2 CC 7.2, SOC 2 A 1.1, SOC 2 PI 1.3

### All 9 active tests — current status
| # | Test | Owner | Status |
|---|------|-------|--------|
| 1 | Azure integration has active Activity Log Alerts | Kalle | OK |
| 2 | Load balancer used (Azure) | Kalle | OK |
| 3 | Azure virtual machine CPU monitored | Max | OK |
| 4 | Virtual machine scale set CPU monitored (Azure) | Max | OK *(was failing — fixed via CAP-7)* |
| 5 | SQL database CPU monitored (Azure) | Kalle | OK |
| 6 | SQL database memory utilization monitored (Azure) | Kalle | OK |
| 7 | SQL database I/O monitored (Azure) | Kalle | OK |
| 8 | SQL database free storage space monitored (Azure) | Kalle | OK |
| 9 | Storage Accounts health monitored (Azure) | Max | OK *(was failing — fixed via CAP-7)* |

Verified directly on Tests pages: both formerly-failing tests show `OK`, `Failing entities: --`, `Due date: —`.

### Azure Monitor — alerts in place

VMSS CPU alerts (RG-scoped, `Percentage CPU > 80`, 15m window, 5m eval, sev 2, action group `ag-alerts-normain-prod-01` → `alerts@normain.com`):
- `vmss-cpu-alert-normain-prod-01` (RG `MC_rg-normain-prod-01_aks-normain-prod-01_swedencentral`)
- `vmss-cpu-alert-normain-dev-20` (RG `MC_rg-normain-dev-20_aks-normain-dev-20_swedencentral`)

Storage availability alerts (15 total, all enabled, all targeting `Availability < 99`, sev 2):
- All 10 originally-flagged: `rps`, `dss`, `aui` × {dev20, prod01}; `normaintfappdevelopment`, `normaintfappproduction`, `normainfglobal`, `normaintfidentity`
- Pre-existing: `uploaded-files-storage-availability-alert-{dev-20, prod-01}`, `storage-availability-alert-{dev-20, prod-01, global-logarch}`

## Solution — CAP-7 dedup

**MON-4 = subset of CAP-7.** The 2 failing tests under CAP-7 (ISO 27001 A.8.6 — capacity management) are the same automated tests Vanta also maps to MON-4 (SOC 2 monitoring). One fix satisfied both. Therefore:

- Do not re-implement Fixes 1/2 from `cdd56b0d` — already applied (verified via `az monitor metrics alert list`).
- MON-4 has **no MON-4-specific evidence slots** (drawer shows "No documents associated") — unlike CAP-7, which has 3 doc slots expiring Jun 2-5, 2026. MON-4 needs no document renewal.
- MON-4 is purely test-driven; the green automated tests **are** the evidence for the auditor.

### Drive verification

Per project rule, only Drive is authoritative for compliance content. MON-4 has no Drive-resident evidence requirements (no document slots in the control). The CAP-7 investigation already ran the relevant `gws drive files list` searches for capacity/monitoring artefacts (none in `kalle@normain.com` accessible Drive); same negative result applies here. No additional Drive search needed for MON-4.

## Actions taken

- Read MON-4 drawer in Vanta (`controlId=system-threshold-monitoring`); confirmed 9/9 OK, 3 frameworks, owner Kalle
- Listed all 9 active tests by name + owner
- Verified both formerly-failing tests (VMSS CPU, Storage Accounts health) on the Tests page — both `OK`, no failing entities, no due dates
- Cross-referenced CAP-7 investigation (`cdd56b0d`); confirmed full overlap with MON-4's failing tests
- Verified Azure Monitor state via `az monitor metrics alert list` — VMSS + storage alerts all present and enabled
- No Azure writes performed
- No Vanta evidence uploaded (none required — control has no document slots)

## Blocked on / open questions

- **Deactivated test** — drawer reports "1 test/document is deactivated"; the "Show deactivated" toggle is not interactable via the browser eval path. Not audit-blocking (9/9 active pass), but worth a manual eyeball before audit.
- **VMSS alert scope quirk** — both VMSS CPU alerts are scoped at the AKS managed RG, not at the VMSS resource directly. Vanta accepts this (test is OK), but if the auditor questions it, switching scope to the VMSS resource id is a 5-min change.
- **CAP-7 doc renewal (Jun 2-5, 2026)** — does NOT affect MON-4 (no doc slots) but is the next compliance risk for the same surface. Tracked under CAP-7.
