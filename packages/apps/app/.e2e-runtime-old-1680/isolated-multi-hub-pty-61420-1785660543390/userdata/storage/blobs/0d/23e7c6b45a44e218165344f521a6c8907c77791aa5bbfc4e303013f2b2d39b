flowchart TD
    subgraph F["Foundations — fixed order"]
        F1["What happens when you prompt an AI (ep. 1)"]
        F2["How models learn: patterns, not rules"]
        F3["Why AI is confidently wrong (hallucination)"]
        F4["Narrow AI vs. general intelligence — what today's AI can't do"]
        F5["It's not one thing: generative vs. recommender vs. decision-making systems"]
        F1 --> F2 --> F3 --> F4 --> F5
    end

    subgraph D["Daily life — grounding"]
        D1["AI you already interact with without knowing it"]
        D2["Generative tools: real uses, real limits"]
        D3["AI at work: automation vs. augmentation"]
    end

    subgraph R["Risks"]
        R1["Bias in automated decisions"]
        R2["Deepfakes and misinformation"]
        R3["Privacy: what happens to what you type"]
        R4["AI-enabled scams and fraud"]
    end

    subgraph I["Big picture / implications"]
        I1["Jobs and the economy: hype vs. realistic"]
        I2["EU AI Act: what it actually means for you"]
        I3["Who controls this, and why that matters"]
        I4["The AGI / superintelligence debate — nobody-knows-yet territory"]
    end

    F5 --> D1
    F5 --> R1
    D --> I
    R --> I

    J["Evaluative toolkit — how to judge a claim<br/>(the four-bucket framework, applied every episode)"]
    J -.-> F
    J -.-> D
    J -.-> R
    J -.-> I
