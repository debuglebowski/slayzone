# Bump protobufjs CVE-2026-41242 — Investigation

## Overview

- **Linear:** [NOR-2590](https://linear.app/normain/issue/NOR-2590/bump-protobufjs-to-755-cve-2026-41242) — moved to **Done** 2026-05-04 (was stuck in "Solved" / `started` type, blocking Vanta).
- **Repo:** `normainofficial/normain`
- **PR:** [#1971](https://github.com/normainofficial/normain/pull/1971) — **MERGED** 2026-04-29 (commit `4f18eefb`), all checks green, review APPROVED.
- **Dependabot alert #153:** state=`fixed`, `fixed_at`=2026-04-29.
- **Blocker (resolved):** Linear ticket was in "Solved" (statusType `started`); Vanta requires `completed`. Moved to "Done" (`c38ec338`).

## Situation

- **CVE-2026-41242** / GHSA-xq3m-2v4x-88gg — protobufjs arbitrary code execution. CVSS 9.8 (Critical).
- **Affected version:** protobufjs 7.5.4 (transitive) in `normainofficial/normain` `yarn.lock`.
- **Consumers:** `@grpc/proto-loader@0.8.0` (^7.5.3), `@opentelemetry/otlp-transformer@0.208.0` (^7.3.0). No first-party direct imports.
- **Fix range:** ≥ 7.5.5. PR bumped to **7.5.6** (also satisfies). Both consumer ranges accept the bump.
- **SLA:** Operations Security Policy = 30d from discovery (2026-04-17 → due 2026-05-17). Vanta P0 test due 2026-05-05.

## Solution

PR was already merged when this investigation started. Ticket transition was the only remaining gap.

```bash
# 1. Confirm PR merged + checks green
gh pr view 1971 --repo normainofficial/normain --json state,mergedAt,reviewDecision,statusCheckRollup
# → MERGED, APPROVED, all SUCCESS

# 2. Confirm Dependabot alert closed
gh api repos/normainofficial/normain/dependabot/alerts \
  -q '.[] | select(.security_vulnerability.package.name=="protobufjs")'
# → state: fixed

# 3. Move Linear ticket to "Done" (statusType: completed)
# via mcp__linear__save_issue with state id c38ec338-7c15-4ba9-b65a-559de46dd09a
```

Vanta agent re-scans periodically; P0 test should clear on next scan now that NOR-2590 is `completed`.

## Actions taken / Blocked on

**Done:**
- Verified PR #1971 merged 2026-04-29, all checks SUCCESS, APPROVED.
- Verified Dependabot alert #153 fixed (protobufjs upgraded to 7.5.6 ≥ required 7.5.5).
- Transitioned NOR-2590: "Solved" → **"Done"** (`completedAt`: 2026-05-04T08:46:12Z).

**Blocked / not done:**
- Linear comment summarizing the close was **denied** by permission policy (external system write not pre-authorized for posting commentary). Ticket transition itself was authorized per task instructions ("Once merged: close Linear ticket"). No comment posted.
- Vanta browser panel not open in this task; did not screenshot the failing test page. Not required for fix — CVE remediation evidence lives in GitHub PR + Dependabot alert + Linear ticket.

**Next:**
- Wait for Vanta agent re-scan to confirm test passes.
