# 🟡 PR 3 — `nor-2605-anthropic-sdk-0.81` Brief

🔗 **Linear ticket:** [NOR-2605](https://linear.app/normain/issue/NOR-2605/lift-anthropic-aisdk-to-0810-to-close-ghsa-5474-4w2j-mq4c-vanta) — _Lift @anthropic-ai/sdk to ^0.81.0 to close GHSA-5474-4w2j-mq4c (Vanta)_

## 📜 Background

`@anthropic-ai/sdk` is a direct dependency in the worker workspace of `normainofficial/normain`:
- `src/apps/server/worker/package.json` (workspace `@normain/worker`)

It currently pins `^0.80.0`. Vanta has **2 open Medium alerts** mapped to this dep:

| 🆔 GHSA | 📊 CVSS | 🔖 Summary |
|---|---|---|
| 5474-4w2j-mq4c | 5.4 | Memory Tool Path Validation Allows Sandbox Escape to Sibling Directories |

(2 alerts because Dependabot reports both the workspace `package.json` and the root `yarn.lock` entry.)

The vuln affects the SDK's **local filesystem memory tool** — sandbox escape to sibling directories via crafted paths.

**Reachability check (informational):** our worker uses the **Anthropic Bedrock SDK** path with `messages.create()`, predefined search + web tools, structured output schema. **No `memory` tool, no local filesystem tool usage.** The vulnerable code path is not exercised. Bumping is still preferred for audit optics — patch is cheap.

## 🎯 Mission

🚀 Open and merge **PR 3: `nor-2605-anthropic-sdk-0.81`** in `normainofficial/normain`. After merge, both Medium alerts auto-close.

📜 **Standards covered:** ISO 27001 A.8.8 · SOC 2 CC7.1
📜 **Internal compliance:** Operations Security Policy v1.0.0 → Change Management

## 🔧 Changes

| 📦 Package | ⏪ From | ⏩ To | 📍 Where | ⚙️ Action |
|---|---|---|---|---|
| 🤖 @anthropic-ai/sdk | `^0.80.0` | `^0.81.0` | `src/apps/server/worker/package.json` | ⬆️ direct bump |

`0.80 → 0.81` is a single-minor bump within a pre-1.0 SDK. Type signatures may shift; tsgo will catch any type errors.

## 🪜 Execution steps

| # | 🪜 Step | 💻 Command / action |
|---|---|---|
| 1 | 🌿 Branch off main | `git checkout -b nor-2605-anthropic-sdk-0.81` |
| 2 | 📖 Skim `@anthropic-ai/sdk` CHANGELOG between 0.80.0 and 0.81.0 | https://github.com/anthropics/anthropic-sdk-typescript/releases |
| 3 | ✏️ Edit `src/apps/server/worker/package.json` → bump `"@anthropic-ai/sdk": "^0.81.0"` | manual |
| 4 | 📦 `yarn install` | verify lockfile diff touches only @anthropic-ai/sdk |
| 5 | 🏗️ `yarn root:build --concurrency=75%` | full monorepo build |
| 6 | 🧹 `yarn root:lint` | no new lint errors |
| 7 | 🔬 `yarn root:ts` | resolve any type errors from SDK signature drift (this is the most likely fix-up area) |
| 8 | 🧪 `yarn root:test` | all tests pass |
| 9 | 🤖 Manual smoke: trigger one Claude-using worker job locally and confirm it completes | run a representative `extractInsightWorker` job |
| 10 | 🚀 Push branch + open PR via `gh pr create` | title: `Lift @anthropic-ai/sdk to ^0.81.0 to close GHSA-5474-4w2j-mq4c (Vanta)` |
| 11 | 📝 Fill `.github/pull_request_template.md` — start the body with `[NOR-2605](https://linear.app/normain/issue/NOR-2605/lift-anthropic-aisdk-to-0810-to-close-ghsa-5474-4w2j-mq4c-vanta)` so Linear's bot auto-links the ticket. Description: GHSA-5474-4w2j-mq4c, mention SDK 0.80→0.81 type drift if any | manual |
| 12 | 👀 Wait for CODEOWNERS review | `@PhilipNgo` or `@redsuperbat` must approve |
| 13 | 🔀 Merge after CI green + approval | per repo convention |
| 14 | ✅ Verify alerts auto-close in GitHub Security tab | expected 2 closures |
| 15 | 🔗 Paste merged PR URL into the parent Vanta task | mark progressed |

## 📐 Repo facts

- 📁 Path: `/Users/Kalle/dev/projects/normain`
- 🧰 Yarn: berry v4.9.2
- 🤖 Worker uses Bedrock SDK path (`eu.anthropic.claude-sonnet-4-6`); SDK calls are `messages.create()` only, no streaming, no memory tool
- 👥 CODEOWNERS: `@PhilipNgo` or `@redsuperbat` must approve PRs from `@debuglebowski`

## ⚠️ Watch for

- 🚨 Pre-1.0 SDK — types for `messages.create`, tool-use blocks, or content-block shapes may have shifted between 0.80 and 0.81. If `yarn root:ts` errors in worker code, fix the type usage rather than pinning around it.
- 🚨 If the Bedrock variant package (`@anthropic-ai/bedrock-sdk` if used) lags behind core SDK, check compatibility.
- 🚨 Watch for any deprecation warnings during a worker job run.

## ✅ Definition of done

- [ ] 🌿 Branch `nor-2605-anthropic-sdk-0.81` opened
- [ ] 📖 CHANGELOG reviewed
- [ ] ✏️ Worker `package.json` updated
- [ ] 🤖 CI green (build + lint + ts + test)
- [ ] 🤖 Manual worker job smoke test passed
- [ ] 👀 CODEOWNERS approval recorded
- [ ] 🔀 PR merged into `main`
- [ ] ✅ 2 Dependabot alerts auto-closed
- [ ] 🔗 PR URL pasted into parent Vanta task

**⏱️ Total effort:** ~30 min – 2 h depending on type drift + reviewer cycle.
