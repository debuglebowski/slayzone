# Slice 3/8 — Finalize IPC→tRPC + REST split — REPORT

## Outcome
**Slice 3 (per-router migration) verified COMPLETE.** No code migration needed —
P2–P22 already added tRPC routers for every domain. Deliverable = verification +
durable decision record. Behavior-preserving; nothing user-visible changed.

- **Commit:** `242de9bf` — `refactor(transport): finalize IPC→tRPC + REST split (P2 followup)` (ARCHITECTURE.md Decision Log entry — only repo edit).

## Scope decision (conflict resolved)
Subtask brief referenced phase-0 / `ea7be1f0` (delete domain IPC, move REST+MCP to
sidecar, "zero domain ipcMain.handle"). **Authoritative parent intent (artifact
`2e715da6`) overrides:** phase-0 = reference only (NOT merging — diverged 426
commits); small behavior-preserving slices; keep IPC+REST for coexistence;
REST/MCP→sidecar = slice 6; drop legacy IPC = slice 8; renderer cutover = slice 5.
User approved SAFE path. Deleting domain IPC now would break the renderer (slice 5
not started) AND the slay CLI (talks to app over REST).

## Audit result
- **ipcMain.handle/on:** 548 across 34 files (non-test). Unchanged — coexistence is intentional until slices 5/8.
- **Domain stragglers (domain op without tRPC router): ZERO.** Every domain handler maps to a router delegating to the same ops.
- **Dead channels (renderer wrapper → no handler): ZERO** (533 preload invoke targets all resolve).

### Channel categorization (4 buckets)
- **tRPC-covered (dual-emit, coexisting):** worktrees, task(+artifacts), ai-config(+marketplace), integrations, terminal (pty/chat/queue), file-editor, projects, tags, automations, settings(+theme), usage-analytics, task-terminals, history, agent-turns, template, test-panel, diagnostics; app-level via `app` router (backup, clipboard, screenshot, leaderboard, exportImport, feedback, files, usage, taskWindows, floatingAgent).
- **Electron-only-IPC (by design — stay):** `browser:*`, `webview:*`, native window lifecycle, `task-window:*`, `pty:ensure-alive:ack`, `warm:setProjectTabCounts`, internal main↔main signals (`db:taskTags:setForTask:done`, `db:tasks:*:done`).
- **app-bootstrap (slice 8):** `app:*` (port/version/zoom/update/focus), `e2e:*`, `window:close` etc.
- **REST-for-CLI:** see below.

## REST endpoints (38) — CLI transport, stay as-is (sidecar move = slice 6)
Task: `/api/tasks` `/api/tasks/:id` `/api/tasks/:id/archive` `/api/tasks/:id/unarchive` `/api/tasks/archive-many` `/api/open-task/:id` `/api/close-task/:id`
Artifacts: `/api/open-artifact/:id` `/api/artifacts/:id/export/{pdf,png,html}`
Automations: `/api/automations/:id/run`
Processes: `/api/processes` `/api/processes/:id` `/api/processes/:id/logs` `/api/processes/:id/follow` (SSE)
PTY: `/api/pty` `/api/pty/start` `/api/pty/respawn` `/api/pty/:id` `/api/pty/:id/{buffer,write,submit,wait}` `/api/pty/:id/follow` (SSE)
Tabs: `/api/tabs/{create,rename,split}`
Browser: `/api/browser/{url,navigate,click,type,eval,content,screenshot,tabs}`
Bus/hooks: `/api/notify` `/api/agent-hook`

## Deferred (with reason)
- **`cli/db.ts` port rename → `slayzone_server_port`:** slice 6 (main's `mcp-server.ts` still owns `mcp_server_port`).
- **`mcp-server.ts` / `mcp-tools/` removal + REST relocation to sidecar:** slice 6.
- **5 orphan preload wrappers** (`git.getFileDiff`, `git.getCommitDag`, `git.getResolvedUpstreamGraph`, `git.getResolvedRecentCommits`, `usage.taskCost` — proven dead, tRPC equivalents exist, zero callers): **slice 8** (whole preload bridge teardown; cherry-picking 5/~500 now = churn).
- **Renderer cutover off `window.api.*`:** slice 5.
- **Drop legacy IPC + preload bridge:** slice 8.

## Sustainability
Safe path matches parent-intent principle (small shippable, behavior-preserving, no
big-bang, single source of truth at the ops layer — routers + IPC both delegate to
the same domain functions). No manufactured doc, no arbitrary partial cleanup.
Durable record = one ARCHITECTURE.md line; exhaustive snapshot = this report.

## Verification
Only repo edit is a Markdown Decision Log line — no compile/runtime surface touched.
Main remains functionally unchanged.
