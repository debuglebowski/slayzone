# Wave 2 — Hub/Runner Integration (Model A)

## Model A (locked)
pty-manager, chat runtime, task ops, ALL DB writes STAY on hub. Only OS-level exec remoted to runner: node-pty spawn/io, child_process spawn, git/fs worktree ops. Wave-1 DB seams keep local db impls (NOT proxied). pty output flows runner→hub as `pty.data{sessionId,seq,data}` notifications re-emitted on hub `ptyEvents` (renderer unchanged). Routing: `resolveTaskRunnerId(db,taskId)` — null/LOCAL = today's in-process path. Gate = `fleet_mode` boot setting; local runner auto-spawned + default → default behavior byte-identical.

## Delivery: same as wave 1 — NO PR/push/branch. Worktree agents → harvest patch → apply → verify → local commit on main.

## PHASE 2A — parallel leaves (path-disjoint, dark, touch NONE of composition.ts/server.ts/router.ts/app-deps.ts/app-main)

**A1 — Terminal PtyBackend seam + spawn-lookups injection + barrel gaps.** New `terminal/src/server/runtime/pty-backend.ts` (PtyBackend/PtyHandle/PtySpawnSpec, `localPtyBackend` wrapping spawnWrappedShell + -l fallback moved from pty-manager.ts:1559-1590, `setPtyBackend`); edit pty-manager.ts (spawn chokepoint ~1094-1096/1559 → `getPtyBackend().spawn(spec)`; node-pty onData ~1868 unchanged), pty-store.ts (inject PtySpawnLookups w/ new `resolveRunnerId(taskId)` instead of hardcoded :77; thread runnerId into CreatePtyOptions), terminal barrel (export setPtySessionLedger/PtySessionLedger/PtyBackend/setPtyBackend/setPtySpawnLookups). Default = localPtyBackend. Test: fake backend drives create/write/resize/kill asserting ptyEvents seq/data/exit unchanged; localPtyBackend real-shell parity. RISK: shell-fallback + sync-query fd path behind seam w/o drift.

**A2 — Fleet exec-proxies + wire contract.** Additive frames.ts (git.*/fs.*/proc.* methods + proc.data/proc.exit notifs; add 'proc.' to RESERVED_HUB_METHOD_PREFIXES; NO version bump); new `fleet/src/server/exec-proxies.ts` (`createRoutingPtyBackend`/`createRoutingProcessBackend`/`createRemoteWorktreeAdapters` — dispatch on spec.runnerId; remote PtyHandle: gateway.request pty.spawn + one pty.data/pty.exit listener + Map<runnerId:sessionId,handle> demux + getBufferSince gap-backfill; worktree adapter forwards 8 methods to git.*/fs.*, keeps getWorktreeColor/ensureProjectWorktreeColors LOCAL (sync), complete 10-method object); barrel. Seam types `import type` only (no cycle). Test: fake in-mem gateway, pty.data w/ gaps → getBufferSince replay + monotonic + exit; worktree/proc forwards. RISK: seq gap-recovery on reconnect; demux-map leak on runner-lost (dispose handles).

**A3 — Processes ProcessBackend seam.** New `processes/src/server/process-backend.ts` (`spawn(spec)→ProcHandle{pid,onData,onExit,kill}`; `localProcessBackend`=child_process.spawn byte-identical, keeps detached/autoRestart); edit process-manager.ts doSpawn ~206-251 through seam; barrel. Remote impl lives in A2. Test: fake backend streaming/autoRestart; real child_process parity. RISK: stdout/stderr interleave + autoRestart timers across seam.

**A4 — Runner exec handlers.** Refactor runner main.ts createHubRequestHandler :40 into dispatch table; new `runner/src/handlers/{pty,git,fs,proc}.ts`; config.ts enforce allowedRoots realpath guard + extend DEFAULT_CAPABILITIES. pty.spawn→node-pty + runner-side RingBuffer assigning seq, stream pty.data/pty.exit via dialer.notify, answer pty.getBufferSince; pty.kill/resize/write; git.*/fs.* under allowedRoots; proc.spawn/kill. Consumes A2 schemas (contract, not file dep). Test: fake dialer sink, real shell, monotonic seq + getBufferSince replay; allowedRoots traversal reject. RISK: runner RingBuffer/seq must mirror hub exactly; path-traversal enforcement.

**A5 — Enrollment/auth adapter factory.** New `apps/server/src/fleet-auth.ts` pure factory `createFleetAuthAdapters({db,auth})→{verifyEnrollment,verifyApiKey}` (verifyEnrollment: verifyJoinToken→registerRunner→mintRunnerApiKey; verifyApiKey: verifyRunnerApiKey→getRunner). Test: real migrated DB + createHubAuth; mint→enroll→assert runner row+key; verifyApiKey resolves; bad-key reject. RISK: verifyJoinToken single-use vs gateway idempotency-on-reconnect — add short re-enroll grace or idempotent register/mint keyed on (tokenId,name).

## PHASE 2B — serial backbone (single-owner hotspots, strictly ordered)

- **B1 (first) composition.ts:** read fleet_mode; when on build hubAuth+fleetGateway(createFleetAuthAdapters) + inject routing seams (setPtyBackend/setPtySpawnLookups after :316, setProcessBackend after :387, configureTaskRuntimeAdapters({worktrees:createRemoteWorktreeAdapters}) complete obj over :217); keep createDbPtySessionLedger (Model A); expose fleetGateway/hubAuth on ServerComposition. Off → verbatim today.
- **B2 server.ts:** loadOrCreateHubIdentity; stand up fleet https+wss (or /fleet path distinct from origin-locked /trpc :142-149) → gateway.handleConnection; mount createAuthExpressApp into muxed handler :111-118; close gateway in stop() :243.
- **B3 router.ts + app-deps.ts:** setRunnersDeps/setFleetGateway (mirror setProcessesDeps); new routers/runners.ts (list+live listRunners, mintJoinToken, setTaskRunner/setProjectDefaultRunner, revokeRunner); register in router.ts :31-60.
- **B4 (last) app/src/main/index.ts + boot-config.ts:** extend BootConfig w/ fleet_mode/default-runner; local-runner supervisor modeled on sidecar spawn :2076-2145 (spawn runner app → hub fleet URL + self-minted join token, supervise/respawn); dark-launched (routing predicate short-circuits default to in-process).

2B strictly serial: B1 constructs gateway/auth handles B2/B3 consume; B4 needs A4 runner + B2 gateway to validate loopback enroll→exec. Verify each end-to-end after prior lands.
