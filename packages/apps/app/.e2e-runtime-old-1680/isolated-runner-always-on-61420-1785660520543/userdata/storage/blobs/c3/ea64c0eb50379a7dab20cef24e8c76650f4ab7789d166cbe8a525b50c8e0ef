# Change-management verification — AWS Bedrock / Anthropic introduction

_v4, 2026-06-12. RESOLVED: INT-408 designated as combined CR + risk assessment (CTO decision); remediation applied in Linear. KPMG out of AWS scope (dedicated mode; AWS is SaaS-only). DPAs to active customers assumed sent (Sara sends; evidence in her mailbox)._

## The change
AWS Bedrock hosts Anthropic models, SaaS open-platform mode only.
- **PR #2180** merged 2026-06-09 (Max authored, independently approved).
- `bedrock.tf` (eu-west-1, EU inference profiles) + migration `shouldUseAnthropic BOOLEAN NOT NULL DEFAULT false` → **dormant until an org is enabled**.

## Verification matrix — final
| Control | Status | Evidence |
|---|---|---|
| Change record (CR) | ✅ | **INT-408** = combined CR + risk assessment. `Change` label added, PR #2180 linked via comment, status Done (all 2026-06-12; closing comment documents the template deviation per procedure's Process Deviations clause). |
| Risk assessment + Security Delegate approval | ✅ | INT-408 created by Max 2026-04-21; "Approved" comment same day by Kalle = CTO = Security Delegate. Creator ≠ approver. 7 weeks pre-implementation. |
| CAB approval | ✅ | CAB ≈ CTO at company size; the recorded approval on the ticket is the CAB record. |
| Rollback plan | ✅ | In INT-408 (per-org flag = rollback mechanism; account teardown). |
| Peer-reviewed implementation, IaC, EU residency | ✅ | PR #2180 independent approval; Terraform; eu-west-1 only. |
| Subprocessor list includes AWS | ✅ | New "SaaS DPA" doc (2026-06-04) has AWS row (AWS EMEA SARL, EEA/EU, SCC). |
| Notice to existing SaaS customers | ✅ assumed | Sara sent new DPAs (user-confirmed assumption). KPMG irrelevant (dedicated mode, no AWS path). |

## Still open (DC 9 finalization, pre-issuance)
1. **Prod check**: confirm no org has `shouldUseAnthropic=true` yet; if enabled, get the enablement date.
2. **DC 9 effective date** = first customer-data flow (flag activation), NOT the 2026-06-09 merge. Confirm at issuance.
3. **DC 9 wording**: "deferred more than 30 days after notice" → align with evidence; DPA §8.1 requires 14 days. Get Sara's send date if the 30-day claim is to stand, else write "in accordance with the DPA's 14-day subprocessor-change notice".
4. dc9-pending-finalization.md: update its bracket text per 2–3 when writing into the Google Doc.

---

# v5 addendum (2026-06-12) — 30-day notice claim SUBSTANTIATED

Found: **`subprocessors-list-2026-04-28.docx`** (Drive `1JmF37dBsbfsXuAjVvBfHrHHxsGyAlrya`, in `…/uc8/data-sources/`, uploaded 2026-05-19 batch). Header: "Normain AB — List of third-party subprocessors. This list was updated: 28 April, 2026". **Table 1 includes AWS** (Amazon Web Services | Search and model infra | EEA/EU; GDPR; SCC | AWS EMEA SARL, Luxembourg).

## Corrected timeline
| Date | Event |
|---|---|
| 2026-04-21 | INT-408 risk assessment created + Security Delegate (CTO) approval |
| 2026-04-28 | Subprocessor list updated with AWS — notice basis (DPAs sent per CTO; Sara sends) |
| 2026-05-19 | Docx snapshot in Drive |
| 2026-06-04 | New consolidated "SaaS DPA" doc (AWS row) |
| 2026-06-09 | PR #2180 merged — **+42 days after list update** |
| pending | Per-org flag activation = customer-data flow = DC 9 effective date |

→ **DC 9's "implementation deferred more than 30 days after notice" HOLDS** (42 days even counting the merge as implementation; more by activation). v3/v4 caution about "14-day vs 30-day" wording is withdrawn; earlier pushback was based on the 2026-06-04 SaaS DPA doc — the actual notice artifact is the 2026-04-28 list.

## Notes
- This list pre-dates the new SaaS DPA format; June 4 doc was consolidation, not first disclosure.
- List includes Sentry (not deployed — "pre-approved kept listed" practice, consistent with DC 3.5 trailing para) and lacks Anthropic (consistent with model-creator framing: AWS processes, Anthropic gets no customer data) and Firecrawl (still a Track B reconciliation item, unrelated to AWS).

## Remaining for DC 9
Only the effective date: capture the date the first org gets `shouldUseAnthropic=true`, drop the bracket, write into the Google Doc, archive dc9-pending-finalization.md.
