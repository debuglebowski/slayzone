# CHG-39 — Change management (ISO 27001 A.8.32) — Investigation

**Task:** CHG-39 · Vanta status: 9/10 tests passing
**Vanta:** https://app.eu.vanta.com/c/normain.com/controls (search CHG-39)

## v3 — re-sourced from Drive

Reverified all compliance claims against Drive (`gws`). Changes from v2:

- ❌ **Removed:** "NRM-CHG-001" internal ID — that came from a local repo file (`current-ucf/control-register.csv`) which is not authoritative. No Drive document references "NRM-CHG-001" or "CHG-39" — these are Vanta's internal IDs only.
- ✅ **Re-grounded:** A.8.32 mapping now confirmed via Drive **Statement of Applicability** spreadsheet (id `1RiHY7SMEkX6l31lSIDredIkqhocvfyz8rg4kDgUS2Dw`), which lists row 8.32 "Change management" as Implemented with evidence tracked in Vanta.
- ✅ **Reverified:** Change Management Procedure policy text — already Drive-sourced in v2, unchanged.
- ✅ **Reverified:** A.5.32 typo flag — Drive SOA confirms the correct control is 8.32 (no 5.32 exists in SOA), so the policy doc's reference to "A.5.32" is definitively wrong.

## ⏳ Blocking dependency

Vanta still shows **Gustav (offboarded)** as control owner. **Kalle must reassign CHG-39** before any evidence work or test re-run will land.

## Authoritative sources (Drive)

| Doc | Drive ID | Purpose |
|---|---|---|
| **Change Management Procedure** (v1.1, last authored 2025-06-25 by Gustav Lidén; copy bumped 2026-04-21 in "KPMG dossier to share") | `1noqgkwdB5zKilDgq8JUKqPhmpBDhj5DSONsk_lgxnWM` | The procedural text auditors will compare against |
| **Statement of Applicability (ISO27001 2022)** | `1RiHY7SMEkX6l31lSIDredIkqhocvfyz8rg4kDgUS2Dw` | Confirms A.8.32 in scope, status "Implemented", evidence = Vanta |

Two near-identical duplicates of the procedure exist in "KPMG dossier to share" — possible audit hygiene issue.

### Key requirements the procedure imposes

| Policy clause (Drive) | Current GitHub state (`gh`) | Gap? |
|---|---|---|
| "All changes must be peer-reviewed" | `normain`: PR + 1 approval required | Partial — bypass actors can skip review |
| "All changes must begin with a Linear CR ticket" | Not enforced in CI | Likely partial |
| **Segregation of Duties: "deployer ≠ approver"** | `normain` ruleset has bypass actors PhilipNgo + redsuperbat with `always` mode → can self-merge | **Likely failing** |
| "Verification in Staging before production" | `deploy-k8s-dev.yml` + `deploy-k8s-prod.yml` exist; ordering not inspected | Probably OK — **unverified** |
| "CI/CD access role-based, time-limited, auditable" | Not inspected | **Unverified** |
| "Production deployment requires ≥1 independent party" | No second-approver / CODEOWNERS | Likely failing |

### Editorial defect in policy

The procedure cites *"ISO/IEC 27001:2022 – Annex A, Control A.5.32 (Change Management)"*. The Drive SOA confirms the correct control is **A.8.32**; A.5.32 does not exist. Trivial 1-character fix in the procedure.

## Situation overview

### What CHG-39 / A.8.32 covers
Controls on changes to information processing facilities and information systems: code (PR review/CI), infrastructure (IaC/deploys), configuration.

### Current GitHub posture (from `gh` API — technical state, not compliance content)

| Repo | Default branch | Protection | Notes |
|---|---|---|---|
| `normainofficial/normain` (app monorepo) | `main` | ✅ Ruleset *"Main branch rules"* active | PR required, **1** approval, squash-only, linear history, required check `🔍 Lint & Test`. **No CODEOWNERS.** **Bypass actors (`always` mode):** PhilipNgo, redsuperbat (Max), Admin role, 1 GitHub App. |
| `normainofficial/normain-infra` (Terraform IaC) | `main` | ❌ **None** | `protected: false`, no rulesets. Last push 2024-12-12 — dormant but still authoritative IaC. |

### Most likely failing Vanta test

Could not read the test name from Vanta UI (browser tooling blocked: chrome-devtools instructed off; slay browser panel needs task tab focused). Best inference, ranked:

1. **"Production code repository requires review by independent party"** — bypass actors with `always` mode means PhilipNgo / redsuperbat can self-merge, violating the procedure's SoD clause. Sibling subtask **#30 (GitHub PR-approval enforcement)** likely covers exactly this.
2. **"Production IaC repository requires PR review"** — `normain-infra` has zero protection.
3. Less likely: missing CODEOWNERS, single-approver count.

**Confirm via Vanta UI** once reassignment lands.

## Solution overview

### Fix
Defer to sibling **#30 (GitHub PR-approval enforcement)** as the primary path — it presumably tightens the bypass list and/or raises required approvals on `normain`. CHG-39 closes as a side effect if that's the failing test.

If Vanta UI confirms `normain-infra` is the gap instead, apply a ruleset on `normain-infra/main` mirroring `normain`'s shape. ~5 min.

### Sequencing

1. **Kalle** reassigns CHG-39 in Vanta (Gustav → current owner).
2. Read failing test name in Vanta — confirm whether the gap is `normain` SoD/bypass, `normain-infra` protection, or something else.
3. If `normain` SoD/bypass → wait for #30 to land; verify CHG-39 turns green.
4. If `normain-infra` → apply minimal ruleset there.
5. **Fix the policy doc:** correct "A.5.32" → "A.8.32" in Drive procedure; consider de-duplicating the two copies in "KPMG dossier to share". Confirm with Kalle before editing — it's an audit dossier artifact.
6. Re-run CHG-39 tests in Vanta; confirm 10/10.

### Effort
🟢 Small. ~30 min post-reassignment. Mostly verification + a 1-character policy correction.

## Actions taken (this session)

- ✅ Inspected branch protection / rulesets on `normainofficial/normain` and `normainofficial/normain-infra` via `gh` API.
- ✅ Listed CI workflows on `normain`; read `pr-checks.yml` (lint+test = the required check).
- ✅ Identified bypass actors on `normain`'s ruleset (PhilipNgo, redsuperbat, Admin role, 1 GitHub App — `always` mode).
- ✅ Located authoritative **Change Management Procedure** in Drive ("KPMG dossier to share").
- ✅ Read full procedure text; cross-referenced against current GitHub state; identified SoD/bypass mismatch as primary gap.
- ✅ Located **Statement of Applicability** spreadsheet in Drive; confirmed A.8.32 in scope, marked Implemented.
- ✅ Confirmed policy doc's "A.5.32" reference is wrong (no such control in SOA).
- ✅ Removed unverified "NRM-CHG-001" internal ID from earlier draft (sourced from non-authoritative local repo).
- ❌ Could not read failing-test name from Vanta UI.
- ⏸️ **No shared changes made.** No ruleset applied, no Vanta evidence uploaded, no Drive doc edited.

## Still unverified

- Exact Vanta test name + required evidence (need UI access post-reassign).
- Whether sibling #30 targets bypass-actor cleanup on `normain` or an org-wide ruleset.
- Whether `deploy-k8s-prod.yml` actually requires staging success / has manual approval gate (workflow YAMLs not yet read).
- Whether CI/CD pipeline access (GitHub Actions secrets, deploy keys) meets the policy's "role-based, time-limited, auditable" bar — not inspected.
- Whether the "Linear CR ticket per change" clause is actively enforced anywhere or aspirational.
- Whether the duplicate Change Management Procedure in "KPMG dossier to share" is intentional or cleanup.
- Whether other compliance claims I made earlier in conversation (control ID `NRM-CHG-001`, evidence-source mapping etc.) survive a Drive-only re-grounding — they're now removed; if a Drive control register exists somewhere I haven't found yet, this can be reverified later.

## Unresolved questions

- Which exact Vanta test fails?
- #30 scope — bypass cleanup on `normain`, or org-wide ruleset?
- Remove duplicate procedure copy now or post-audit?
- Fix A.5.32 → A.8.32 typo now or wait til post-audit?
