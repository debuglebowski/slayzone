# Fix: Azure metrics dashboards (dev + prod) — broken tiles

Both Terraform-managed dashboards have 6/9 tiles showing **"Resource not found"**:
- `dashboard-metrics-normain-dev-20`
- `dashboard-metrics-normain-prod-01`

They reference resources that no longer exist after the **Container Apps → AKS** migration and the storage-account split. Both dashboards are tagged `source: terraform`, so this needs to be fixed in the **infra Terraform repo** (portal edits get reverted on next apply).

## What to change (same in both env modules)

**1. Container App tiles → AKS** (3 tiles: CPU, Memory %, Memory usage)
- From: `Microsoft.App/containerApps/aca-normain-backend-api-{env}` (deleted — no container apps exist)
- To: `Microsoft.ContainerService/managedClusters/aks-normain-{env}`
- Metrics: `node_cpu_usage_percentage`, `node_memory_working_set_percentage`, `node_memory_working_set_bytes`

**2. Storage tiles → real account** (3 tiles: Availability, Transactions, Used Capacity)
- From: `Microsoft.Storage/storageAccounts/stwinvmnormain{env}` (deleted)
- To: a current account — dev options: `artnormaindev20 / auinormaindev20 / dssnormaindev20 / ufnormaindev20` (prod: `…normainprod01`)

**3. PostgreSQL tiles** — already correct (`psql-normain-{env}`), no change.

## Need from you
1. Which storage account should the storage tiles track? (`uf…` = uploaded files, likely primary — or show all?)
2. Path to the Terraform module that defines these dashboards.
