# Slice 3/8 — worktrees router (P14, MEGA) — DONE

**Commit:** `637394dc` (direct main, no PR)
`feat(transport): worktrees router (P14 — mega; 84 procedures + 2 subs)`

## Delivered
- **84 procedures + 2 subscriptions** mirrored 1:1 from the 86 `git:*` IPC handlers (largest domain).
- Registered `worktrees: worktreesRouter` in `appRouter`.
- New `@slayzone/worktrees/server` barrel re-exports the git/gh ops the router wraps.
- Added `@slayzone/worktrees` to transport deps (auto-excluded from externalize via app dep — no vite change).
- **Additive only** — IPC handlers + renderer untouched (slice 5 owns cutover).

## TypedEmitter / subscription location
`packages/shared/transport/src/server/routers/worktrees.ts` — bottom of file:
- `onDiffChanged`  → `observable` over `getGitWatcher().on('git:diff-changed')`
- `onDiffWatchFailed` → `observable` over `getGitWatcher().on('git:diff-watch-failed')`

`getGitWatcher()` is the singleton `GitWatcher` (EventEmitter) at
`packages/domains/worktrees/src/main/git-watcher.ts` — same agent-turns pattern (7eef7fb4).
`.off` in teardown → no listener leak (cancellation verified by test).

## Adaptations vs ref d9fedfb6
- `resolveCopyBehavior` / `resolveSubmoduleInitBehavior` are now async → `createWorktree` awaits them.
- `git:createWorktree:phase` progress left IPC-only this slice (no `event.sender` in tRPC ctx) — matches ref.

## Files
- `packages/domains/worktrees/src/server/index.ts` (new barrel)
- `packages/shared/transport/src/server/routers/worktrees.ts` (new, 84 procs + 2 subs)
- `packages/shared/transport/src/server/routers/worktrees.test.ts` (new — sub forward + no-leak)
- `packages/shared/transport/src/server/router.ts` (register)
- `packages/domains/worktrees/package.json` (+`./server` export)
- `packages/shared/transport/package.json` (+worktrees dep)

## Verification
- `pnpm typecheck` ✓ (full monorepo incl. app node+web)
- `pnpm lint` ✓ (0 errors; 226 pre-existing react-hooks warnings)
- sub test ✓ 3/3 (onDiffChanged/onDiffWatchFailed forward emits; teardown removes listener)
- `pnpm build` ✓ — router present in app main bundle
- `pnpm test:e2e` — 783 passed. 03-projects + 25-panel-resize failures **proven pre-existing**:
  identical failures with the router un-registered (caused by parallel commit `c3023714`
  react-hooks refactor, not this slice). Browser/agent-lock failures were flaky (passed on re-run).
