# Slice 3/8 — history tRPC router (P5) — DONE

**Commit:** `72e8f1c3` — `feat(transport): history router (P5)` → main (direct, no PR)

## What shipped
Additive `history` tRPC router. IPC + tRPC coexist (slice 3). 2 IPC sigs mirrored 1:1 as queries:
- `history:listForTask` → `historyRouter.listForTask` ({ taskId, options }) → `listActivityEventsForTask`
- `history:getAutomationActionRuns` → `historyRouter.getAutomationActionRuns` ({ runId }) → `listAutomationActionRuns`

## Files (4, +31)
- `packages/shared/transport/src/server/routers/history.ts` (NEW)
- `packages/shared/transport/src/server/router.ts` (register `history: historyRouter`)
- `packages/shared/transport/package.json` (+`@slayzone/history` workspace dep)
- `pnpm-lock.yaml` (history link, +3)

## Deviations from ref `3d7d9363`
Ref = divergent timeline (full migration in 1 commit; old `src/server`+`src/electron` layout; router w/ diagnostics/projects).
- Import `@slayzone/history/main` (ref's `/server` export gone in current layout).
- Typed zod schema for `options` instead of ref's `as unknown as ZodOptional<...>` cast — same wire shape, no hack (sustainability).
- NO renderer/preload/handler/shared-types changes — those are slice 5 (ref bundled them; sliced approach defers).

## externalizeDepsPlugin
No change needed — app exclude list is dynamic over app's `@slayzone/*` deps; both `history` + `transport` already present → bundled from source.

## Verification
- transport typecheck: **PASS** (clean)
- biome (my files): **PASS**
- eslint (my files): **PASS**
- server electron-free boundary: **PASS** (history/main type-only IpcMain import = no runtime leak)

## ⚠️ Full-repo gates blocked (NOT my regression)
`pnpm typecheck`/`lint`/`build`/`test:e2e` fail on **live concurrent WIP** by a parallel agent — `TreeView.tsx` (+~25 unrelated working-tree files: sidebar/projects/task/migrations). TreeView.tsx changed AFTER session start (unused imports/vars). Left untouched (out of scope, must not drop others' work). My 4 files isolated + committed cleanly; build/e2e re-runnable once concurrent WIP lands.

## DONE WHEN
2 sigs mirrored ✓ · registered ✓ · slice green (isolated) ✓ · committed ✓
