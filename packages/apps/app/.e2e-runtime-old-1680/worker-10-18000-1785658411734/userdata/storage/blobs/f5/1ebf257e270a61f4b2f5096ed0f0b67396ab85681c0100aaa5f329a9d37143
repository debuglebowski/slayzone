flowchart LR
  subgraph Engine["engine — exists, no changes"]
    F["Contact field<br/>kind=relation<br/>target=Contact (self)<br/>cardinality=one"]
    R[("relations table<br/>fieldId / fromId / toItemId")]
    F --> R
  end
  subgraph Web["web — the new part"]
    M["ConceptView display mode:<br/>Table ⇄ Hierarchy"]
    B["tree build (client)<br/>edges = relations of chosen self-relation field<br/>roots = nodes w/o outbound edge<br/>cycle + multi-parent guard"]
    L["dagre TB layout<br/>graphLayouts.ts — exists"]
    X["@xyflow/react canvas<br/>pattern: ConceptGraphCanvas — exists"]
    M --> B --> L --> X
  end
  R --> B
