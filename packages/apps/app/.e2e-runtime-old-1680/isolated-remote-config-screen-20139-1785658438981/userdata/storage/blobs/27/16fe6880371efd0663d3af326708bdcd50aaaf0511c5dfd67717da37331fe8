# Phase 0 — Open Questions

Answer before exec. Format reply: `1a, 2b, 3a, 4b, 5a`.

---

## 1. Single-instance lock under `SLAYZONE_DB_DIR`

Electron's lock keys on binary, not data dir → 2nd `pnpm dev` dies even with isolated dir. Blocks smoke test.

- (a) Skip lock when `SLAYZONE_DB_DIR` set. One-line.
- (b) Re-key lock on data-dir hash. Overlaps Phase 3 single-writer lockfile.
- (c) Defer, CI-only.

**Rec: a.**

---

## 2. `SLAYZONE_PORT` reserve now?

No tRPC bind exists yet (Phase 2/3). Only MCP today.

- (a) Add helper now, dead until Phase 3.
- (b) Defer.

**Rec: b.**

---

## 3. `SLAYZONE_HOST` default + warning

MCP hardcodes `127.0.0.1`. Honoring env opens LAN surface w/o auth (auth deferred to scope 2 per §11b).

- (a) Default `127.0.0.1`. `console.warn` on non-loopback bind.
- (b) Default `127.0.0.1`. Reject non-loopback until Phase 3.
- (c) Default `localhost`. Warn same as a.

**Rec: a.**

---

## 4. CLI `SLAYZONE_DB_PATH` (file) vs `SLAYZONE_DB_DIR` (dir)

CLI uses different env. Two vars same concept = debt.

- (a) Leave CLI.
- (b) CLI honors `_DIR` when `_PATH` unset, derive file.
- (c) Deprecate `_PATH`. Breaking.

**Rec: b.**

---

## 5. `db/index.ts:65` `appData` legacy migration

Pre-rebrand migration reads Electron-default userData. Replacing w/ `getDataRoot()` would relocate old data into dev dir = wrong.

- (a) Skip. Keep `app.getPath()`. Comment why.
- (b) Replace.

**Rec: a.**
