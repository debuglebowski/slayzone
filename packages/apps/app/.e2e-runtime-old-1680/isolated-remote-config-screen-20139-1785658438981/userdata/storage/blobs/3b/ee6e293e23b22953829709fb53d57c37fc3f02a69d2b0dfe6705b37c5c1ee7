# Backup Evidence — Azure Database for PostgreSQL (Production)

**Vanta test:** Backups completed for other databases
**System:** psql-normain-prod-01 (system of record)
**Collected:** 2026-06-17 by Kalle Hansson
**Source:** Azure Resource Manager via `az postgres flexible-server show` (same backend as the Azure portal Backup blade)
**Subscription:** Normain (a9049f9f-3d1b-43f0-9278-7f1d603f6a00)
**Resource group:** rg-normain-prod-01 · **Region:** Sweden Central

## Backup settings

| Setting | Value |
|---|---|
| Server | psql-normain-prod-01 (PostgreSQL v16, Ready) |
| FQDN | psql-normain-prod-01.postgres.database.azure.com |
| Automated backups | Enabled (platform-managed, continuous PITR) |
| Backup retention | 14 days |
| Earliest restore point | 2026-06-03T15:18Z (= full 14-day window today → backups running & complete) |
| Geo-redundant backup | Disabled |
| Storage auto-grow | Enabled (64 GB, P6 tier) |
| High availability | Disabled |

Azure Database for PostgreSQL flexible server performs automated, platform-managed
backups with point-in-time restore. The presence of an `earliestRestoreDate` exactly
14 days back confirms backups are being taken and retained per the configured policy.

## Command run to produce this export

```bash
az account show --query "{sub:name, id:id, user:user.name}" -o json
az postgres flexible-server show \
  --resource-group rg-normain-prod-01 \
  --name psql-normain-prod-01 \
  --output json
```

Authenticated as kalle@normain.com against subscription Normain (a9049f9f-3d1b-43f0-9278-7f1d603f6a00).

## Raw configuration export (az CLI)

```json
{
  "name": "psql-normain-prod-01",
  "location": "Sweden Central",
  "version": "16",
  "state": "Ready",
  "sku": { "name": "Standard_B2ms", "tier": "Burstable" },
  "storage": { "autoGrow": "Enabled", "iops": 240, "storageSizeGb": 64, "tier": "P6" },
  "backup": {
    "backupRetentionDays": 14,
    "earliestRestoreDate": "2026-06-03T15:18:35.939789+00:00",
    "geoRedundantBackup": "Disabled"
  },
  "highAvailability": { "mode": "Disabled", "state": "NotEnabled" },
  "replica": { "role": "Primary" },
  "fullyQualifiedDomainName": "psql-normain-prod-01.postgres.database.azure.com"
}
```
