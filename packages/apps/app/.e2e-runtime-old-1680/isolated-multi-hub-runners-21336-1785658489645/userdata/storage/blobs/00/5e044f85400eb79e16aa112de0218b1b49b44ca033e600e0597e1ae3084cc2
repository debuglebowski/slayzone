# Plan — Consolidate SLA / DPA / Subprocessor structure (Drive)

_Live state as of 2026-06-03. Shared drive: **Normain** (`0ADHH7JidkICFUk9PVA`). No file is deleted: moves within the shared drive preserve file ID, link, and version history; superseded versions keep their bare-date folder or go to `99 - Old/`. Already-signed agreements in customer folders are frozen historical records — never edited or moved._

## Agreed design decisions

| # | Decision |
|---|---|
| 1 | `01 - SaaS default agreements` holds **exactly three folders — Privacy Policy, Cookie Policy, Terms of Use** — each a single master (moved from InfoSec) versioned with the dated-snapshot convention. No DPA/SLA bundles here. |
| 2 | **Marketplace (privacy-first) and SaaS (experimentation-first) are separate lineages** — kept apart even while currently identical. |
| 3 | **Version unit = the whole contract package**, snapshotted together. A snapshot is cut when the **offer/agreements change materially** — not per customer, not per minor edit. To change: copy the latest snapshot forward into a new dated folder and edit there. |
| 4 | Templates split by motion: **`01 - Microsoft Marketplace Template`** + **`02 - Customer Cloud Hosting Template`** (singular). All package content lives **inside dated snapshot subfolders**. |
| 5 | **Every document carries its own in-doc effective/version date** (DPA + SLA header; each subprocessor appendix its own "last updated"). That date **MUST equal the snapshot folder name** (`YYYY-MM-DD`). The signed copy in a customer folder is therefore self-describing — it *is* the per-customer version record; no external sheet is the source of truth. |
| 6 | **All DPA templates embed the subprocessor list as an appendix** (the embedded appendix is the customer-facing source of truth; the InfoSec standalone list is the internal working master that feeds them). Appendix structure standardized across templates; the empty appendix populated. Any template change is **dated today** and created in **`… - DRAFT`** first, promoted to `… - LATEST` on finalisation. Already-signed DPAs are not changed. |

## Snapshot naming convention

- `YYYY-MM-DD - LATEST` = active version · `YYYY-MM-DD - DRAFT` = work in progress · `YYYY-MM-DD` (bare) = superseded history.
- **Lifecycle:** `… - DRAFT` → finalize (rename to `… - LATEST`, drop `- LATEST` from the prior active) → superseded (drop suffix → bare date).
- **Invariant:** exactly **one** `- LATEST` per motion folder; zero-or-more `- DRAFT`; all others bare-date.
- The in-doc effective date (decision 5) must match the folder date.
- This convention + the copy-forward / never-edit-a-used-snapshot rule lives **once** in `05 - InfoSec / 03 - Procedures & Runbooks` (extend the existing policy-update runbook) — not duplicated per folder.

## Snapshot contents (per motion)

| Motion | Each dated snapshot contains |
|---|---|
| Microsoft Marketplace | The Order TEMPLATE · Marketplace DPA (with embedded subprocessor appendix) · SLA copy |
| Customer Cloud Hosting | Normain - Main Agreement · Attachment 1 Terms · Attachment 2 SLA · Attachment 3 DPA (with embedded subprocessor appendix) |

The subprocessor list is embedded **inside** the DPA (decision 6), so it is not a separate file in the snapshot. The **NDA** lives in its own versioned folder `03 - NDA/`; the **SLA calculator** (a tool) in `98 - Tools/`.

## Subprocessor embedding — current variance (to standardize)

| DPA | Clause | Appendix | Content |
|---|---|---|---|
| Marketplace (`1ticl`) | "attached … (Appendix B)" | **Appendix B** | Full: Table 1 (BetterStack, Azure, PostHog) + Table 2 (14 company subs incl. Anthropic) |
| Cloud Hosting template (`14JrU`) | "provided … upon request" | **Appendix A** | Reduced: Table 1 only (BetterStack, PostHog — no Azure) |
| InfoSec hosting (`1it8`) | "attached … (Appendix A)" | **Appendix A** | **Empty / placeholder — no table populated** |

Plus a standalone `List of preapproved subprocessors_2026-04-28` (InfoSec) = a 4th, non-embedded copy. Cloud-Hosting having fewer subprocessors is legitimate (Azure is the customer's). Problems to fix: the **empty appendix**, the **drifting standalone**, and the inconsistent appendix **letter**.

## Current state — `04 - Contracts & Agreements`

```
04 - Contracts & Agreements/
├── 00 - Customer agreement Templates/
│   ├── 01 - Microsoft Marketplace Templates/   (plural — singular rename pending)
│   │   ├── Data Processing Agreement for Microsoft Marketplace
│   │   ├── The Order TEMPLATE
│   │   └── old/
│   ├── 02 - Customer Cloud Hosting Templates/   (plural — singular rename pending)
│   │   ├── Normain - Main Agreement
│   │   ├── Attachment 1 - Terms of Service
│   │   ├── Attachment 2 - SLA                ⚠ old 90%
│   │   ├── Attachment 3 - Customer hosting DPA          (14JrU)
│   │   └── Attachment 3 - DPA_Normain hosting ⚠ dup     (11xa)
│   ├── 03 - NDA/        → NDA mutual TEMPLATE
│   ├── 98 - Tools/      → SLA calculator
│   ├── 99 - Old/        → .docx originals (from Roger)
│   └── [DRAFT — 2026-05-29] Normain SLA   (loose at root → becomes a DRAFT snapshot)
├── 01 - SaaS default agreements/   → EMPTY (to populate)
├── 02 - Customer agreements (drafts and signed)/   → ~28 customer folders (unchanged)
└── old/
```

Masters / records that stay outside this folder:
- **Subprocessor working master** → `05 - InfoSec / 05 - Vendor Management / 00 - PII Sub-processor Management / List of preapproved subprocessors_2026-04-28`.
- **Hosting DPA — editable copies:** the 2 above + InfoSec `02 - Official Other Policies / 2. DPA / Normain - DPA_Normain hosting` (2026-03-18). **Signed PDF** (executed evidence) sits in the same InfoSec `2. DPA` folder.
- **Canonical 99% SLA** → `02 - Customer agreements / KPMG / Final agreements 2025-12-17`.
- **Policy masters to move (decision 1)** → `05 - InfoSec / 02 - Official Other Policies / 1. Terms of User, Privacy Policy & Cookie policy`.

## Target structure

```
04 - Contracts & Agreements/
├── 00 - Customer agreement Templates/
│   ├── 01 - Microsoft Marketplace Template/
│   │   ├── 2026-06-03 - LATEST/   → Order TEMPLATE + Marketplace DPA (embedded appendix) + SLA copy
│   │   ├── 2026-04-20/            → prior snapshot
│   │   └── old/
│   ├── 02 - Customer Cloud Hosting Template/
│   │   ├── 2026-06-03 - LATEST/   → Main Agreement + Att 1 Terms + Att 2 SLA + Att 3 DPA (embedded appendix)
│   │   └── 2025-10-29/            → prior snapshot
│   ├── 03 - NDA/
│   │   └── 2025-06-07 - LATEST/   → NDA mutual TEMPLATE
│   ├── 98 - Tools/                → SLA calculator
│   └── 99 - Old/
├── 01 - SaaS default agreements/        (published policy masters, moved from InfoSec)
│   ├── Privacy Policy/
│   │   └── YYYY-MM-DD - LATEST/   → Privacy Policy
│   ├── Cookie Policy/
│   │   └── YYYY-MM-DD - LATEST/   → Cookie Policy
│   └── Terms of Use/
│       └── YYYY-MM-DD - LATEST/   → Terms of Use
├── 02 - Customer agreements (drafts and signed)/   (unchanged)
└── old/
```

## Steps

1. Rename motion folders to singular: `01 - Microsoft Marketplace Template`, `02 - Customer Cloud Hosting Template`.
2. **Double-check the subprocessor working master** — review `00 - PII Sub-processor Management / List of preapproved subprocessors_2026-04-28`: confirm it is current, the authoritative superset, and the basis for every embedded DPA appendix.
3. **Standardize DPA appendices (decision 6)** — make every DPA template embed the subprocessor list; align the appendix letter/structure; **populate the empty InfoSec hosting appendix**. Because this is a change, date the new versions **today (2026-06-03)** and create them in `… - DRAFT` first.
4. **Reconcile the hosting DPA to one editable master (Q1)** — of the 3 editable hosting-DPA Google Docs (`14JrU`, `11xa`, InfoSec `1it8`), keep ONE as the canonical template; move the other 2 editable copies to `99 - Old/` (or version history) so only one is editable. The **signed PDF in InfoSec `2. DPA` is frozen executed evidence — leave it where it is; do not edit, archive, or delete it.**
5. **Marketplace snapshot** — create `2026-06-03 - DRAFT/` (→ LATEST on finalisation); move the Marketplace DPA + Order TEMPLATE in; add SLA copy. Each doc's in-doc date set to 2026-06-03 to match the folder (decision 5).
6. **Cloud Hosting snapshot** — create the snapshot holding Main Agreement + Att 1 + Att 2 SLA + the canonical Att 3 DPA; same date-match rule. Non-canonical Att 3 → `99 - Old/`.
7. **SLA canonicalisation (Q4)** — the `[DRAFT — 2026-05-29]` SLA is the new draft; once finalised it is the SLA copy in the `- LATEST` snapshots; the old 90% → bare-date history / `99 - Old/`.
8. **Populate `01 - SaaS default agreements`** — create the three policy folders (Privacy Policy, Cookie Policy, Terms of Use); move each policy master from InfoSec into its `YYYY-MM-DD - LATEST/` snapshot; leave optional shortcuts in InfoSec.
9. **Clean up InfoSec + readme** — after the policy + hosting-DPA moves, leave a redirect/shortcut in the emptied InfoSec `1. Terms…` and reconciled `2. DPA` folders; **update the `InfoSec Folder Structure (readme)`** (it documents `05 - InfoSec` only); record the snapshot naming + copy-forward rule once in the policy-update runbook and repoint its folder references.
10. **Verify date↔folder match (decision 5)** — final pass: every snapshot's in-doc effective date equals its folder name.

## Unresolved questions

| # | Question |
|---|---|
| Q1 | Hosting DPA: which of the 3 editable copies (`14JrU`, `11xa`, InfoSec `1it8`) becomes the one canonical template? |
| Q3 | `01` holds only the 3 policies; template motions are Marketplace + Cloud Hosting. Decision 2's **"SaaS DPA (experimentation-first)"** has no home — is the self-serve SaaS product governed *only* by the 3 published policies (no separate DPA/SLA), or does SaaS-open need its own motion + DPA/SLA? |
| Q3b | "Terms" title — `Terms of Use` vs `Terms of Service`? Match the master doc moved from InfoSec. |
| Q4 | Canonical SLA = the 99% (KPMG 2025-12-17) or the 2026-05-29 draft once finalised? |
| Q5 | Confirm mode↔subprocessor mapping: Marketplace/privacy-first = dedicated tenant, **without** Anthropic; SaaS/experimentation-first = open multi-org, **with** Anthropic. |
