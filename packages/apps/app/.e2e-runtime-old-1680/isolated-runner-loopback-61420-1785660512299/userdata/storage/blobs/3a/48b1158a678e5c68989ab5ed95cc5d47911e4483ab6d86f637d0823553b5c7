# VPM-1 — Service infrastructure maintained

**Vanta control:** [VPM-1](https://app.eu.vanta.com/c/normain.com/controls?controlId=support-infra-patched)
**Frameworks:** SOC 2 CC 6.6, CC 6.8, CC 7.2, CC 7.4, CC 8.1
**Owner:** Kalle Hansson
**Status when investigated:** 17/20 OK (3 failing) — drifted from brief's 18/20

## Situation

Brief said 2 failing. Actually **3 failing**: 1 test + 2 documents. Azure-side fully clean — failures are GitHub-side + missing paperwork.

### Failing items

| # | Type | Item | Detail |
|---|------|------|--------|
| 1 | Test | High vulnerabilities identified in packages are addressed (GitHub Repo) | 17 Dependabot vulns in `normain` repo. Several **due today / +1d** (npm: path-to-regexp, effect, tar ×7, lodash, …). Identical scope to sibling **#29**. |
| 2 | Doc | Sample of remediated vulnerabilities | Quarterly. Wants tickets/reports showing high-sev findings remediated (timestamps, descriptions, remediation owners). |
| 3 | Doc | Vulnerability scan | Quarterly. Wants screenshot of latest scan listing assets + vuln types + severities. |

Both docs marked "Upload during SOC 2 observation window", recurrence = quarterly.

### Azure / OS patching state

**All 8 Azure tests pass** — Critical/High/Med/Low for both Azure Container and Azure Server, plus "Vulnerability scanning is enabled (GitHub)". The brief's "Azure Update Manager / OS patch state" angle is a red herring — Vanta is happy with Azure-side patching today.

### Drive / Repo

- No "Patch Management Policy" doc in Drive (searched name+fullText for `patch`, `VPM`, `Update Manager`, `Azure patch`).
- `current-ucf/inventory/operations_security.txt` already contains patch-policy text (vuln severity → ticket → SLA-bound remediation; "all critical/high patches applied within SLA"). SLAs themselves not numerically specified in excerpt.
- `PLAN/phase-1-domains/infrastructure.md` is a stub.
- No code-side patch automation.

## Solution

| # | Failing item | Fix | Effort | Dedup |
|---|---|---|---|---|
| 1 | Test: GitHub High vulns | **Defer to sibling #29** — same 17 CVEs, same repo. VPM-1 auto-greens when #29 closes them. Bump #29 priority — items due today. | 0 here | #29 owns it |
| 2 | Doc: Sample of remediated vulnerabilities | Export GitHub Security → Dependabot **closed/fixed** alerts for the SOC 2 window (CSV/PDF; resolved date + merging-PR owner). Best done **after** #29 closes some. Until then, submit currently-closed alerts. | ~15 min | Output of #29 |
| 3 | Doc: Vulnerability scan | Screenshot GitHub Security → Dependabot alerts overview (asset = repo, vuln type, severity column). Can do today. | ~5 min | None |

**Net VPM-1-specific effort:** ~20 min of doc work. Test failure resolves automatically once #29 lands.

**Sequencing:** doc #3 today → #29 burns down 17 vulns → doc #2 from closed-Dependabot export. Both docs recur quarterly → consider scripting next cycle via `gh api`.

## Actions taken

- Opened Vanta VPM-1 detail panel via `slay tasks browser`; expanded test list; clicked failing items to read evidence requirements.
- Confirmed Azure-side fully passing — no Azure Update Manager work needed.
- Searched Drive (Drive API via `gws`) for patch-mgmt docs — none.
- Reviewed `current-ucf/inventory/operations_security.txt` — patch policy language exists, but SLA numbers not surfaced.
- Inspected sibling tasks #11 / #29 / #33 to confirm dedup with the failing test (#29 = same 17 vulns).

**Not taken (stop for shared changes):** no Vanta uploads, no GitHub PR work, no policy edits.

## Open questions

- Want me to grab the screenshot via `gh` Dependabot output, or do it from GitHub UI?
- Tools beyond Dependabot to reference (Snyk, Defender)?
- Bump #29 to P1/P2 — items due today?
