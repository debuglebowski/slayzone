# Phase 0 — Remaining failing tests (post round 14)

## ✅ Round 14 fixed
- `90-terminal-fd-limit:34` — `expect.poll(...).not.toMatch(/regex/)` errored on number returns; rewrote to return string sentinel ('ok' / 'too-low:N')
- `72-browser-view-lifecycle:184` (Bug 1) — stale `border border-neutral` class → `bg-tab-active`
- `77-browser-view-focus:89` glow — added IPC listener in TaskDetailPage to mirror `browser-view:focused` onto `setFocusedPanel` (source patch)
- `61-web-panel-handoff-routing:182` (1 of 3) — `panel-settings` now defaults to `mod+j`, conflicts with web panel shortcut. Switched test panel shortcut to 'y'. Replaced webview-tag URL helper with DOM URL display.

## Still failing — sorted by effort

### 🔴 Large (3)
- `93-artifacts-panel` ×7 — test-state ordering (rename/delete/empty folder). Bigger rewrite needed.
- `61-web-panel-handoff-routing:226` (cross-host popups) — synthetic webview new-window event doesn't translate to WebContentsView; needs popup IPC simulation
- `71-webview-popup-handling:540/696` — same root: window.open() in WebContentsView opens real BrowserWindow not a popup-event hook the test can capture

## Tally
🟢 0 · 🟡 0 · 🔴 3 → **3 rows / ~12 tests**

Cumulative: **80 → ~12 (~85% reduction)**.
