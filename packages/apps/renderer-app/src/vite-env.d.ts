/// <reference types="vite/client" />
// Gives ambient types for Vite virtual modules (`*?worker`, `*?raw`, etc.) so
// transitively-compiled domain source (e.g. @slayzone/worktrees' worker import)
// typechecks from this package too.
