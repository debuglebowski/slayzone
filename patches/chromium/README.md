# patches/chromium/

`git am`-formatted patches applied to `chromium/src/` in lexicographic order
by `scripts/chromium/apply-patches.sh`.

| # | Name | Phase | Purpose |
|---|------|-------|---------|
| 0001 | `slayzone-rebrand-Chromium-SlayZone` | 2.1 | Placeholder text-only rebrand: `chrome/app/theme/chromium/BRANDING` + `chromium_strings.grd` → "SlayZone". Icon art stays upstream until Phase 19. |
| 0002 | `slayzone-chrome-scheme-handler-factory` | 2.2 | `chrome/browser/ui/webui/slayzone/slayzone_webui_registry.{h,cc}` — single `RegisterSlayzoneWebUIConfigs()` entrypoint called from `RegisterChromeWebUIConfigs()`. Registers `chrome://slayzone-shell/` as the first tenant (empty placeholder body). Every Phase 5+ region lands a `WebUIConfig` here. |
| 0003 | `slayzone-sidecar-client` | 2.3 | `chrome/browser/slayzone/sidecar_client.{h,cc}` — browser-process client for the Bun sidecar. LSP-framed JSON-RPC 2.0 over UDS at `$SLAYZONE_RUNTIME_DIR/sidecar.sock` (platform default `~/Library/Application Support/SlayZone/run/` on mac, `$XDG_RUNTIME_DIR/slayzone/` on linux). Keepalive 30 s, reconnect w/ exp backoff 500 ms → 30 s cap, id-correlated pending map. Started from `BrowserProcessImpl::Init()`, stopped in `StartTearDown()`. |
| 0004 | `slayzone-telemetry-strip` | 2.4 | Surgical hard-offs: `ChromeMetricsServiceAccessor::IsMetricsAndCrashReportingEnabled` → `false`; `MetricsService::EnableRecording` early-return; `VariationsService::IsFetchingEnabled()` → `false` (kills `variations.googlezip.net` phone-home). Pairs with `safe_browsing_mode=0`, `disable_fieldtrial_testing_config=true`, `use_official_google_api_keys=false` in `scripts/chromium/args/release.gn`. |
| 0005 | `slayzone-NTP-chrome-slayzone-shell-rewrite` | 2.5 | `chrome/browser/chrome_content_browser_client.cc` — `HandleSlayzoneNewTabRewrite` registered first in `BrowserURLHandlerCreated`; every new-tab path (address bar, new-tab button, window.open, session restore, extensions) redirects `chrome://newtab/` → `chrome://slayzone-shell/`. |
| 0006 | `slayzone-pin-kCGImageByteOrder32Little` | 1.3 / SDK | Replaces `kCGImageByteOrder32Host` with its little-endian form at 9 call sites. Apple removed the Host alias from Xcode 16.x CGImage.h; it exists only in Xcode 26+ SDKs. Safe because Chromium already asserts `SK_CPU_LENDIAN` around the mac site. Drop once toolchain baseline is Xcode 26+ (requires macOS 15+ host). |
| 0007 | `slayzone-avoid-macOS-26-NSScreen-CGDirectDisplayID` | 1.3 / SDK | `ui/display/mac/screen_utils_mac.mm` references `screen.CGDirectDisplayID` inside an `@available(macOS 26, *)` block — runtime gate is fine, but Xcode 16.x SDKs don't declare the property so it won't compile. Use the `deviceDescription[@"NSScreenNumber"]` fallback unconditionally (works on every macOS version). Drop once toolchain baseline is Xcode 26+. |
| 0014 | `slayzone-Phase-6.1-tabs.mojom-SlayzoneTabsHost` | 6.1 | `chrome/browser/slayzone/mojom/tabs.mojom` (mirror of outer `packages/shared/mojo-bindings/mojom/tabs.mojom`) + `chrome/browser/slayzone/slayzone_tabs_host.{h,cc}`. One `SlayzoneTabsHost` per `SlayzoneBrowserView`; observes `TabStripModel` + per-tab title/URL/load; fans out full `TabSnapshot` lists to every subscribed `TabObserver`. Stable ids via `tabs::TabHandle::raw_value()`. Nav methods (Load/Back/Fwd/Reload/Stop) act on active tab; Create via `chrome::AddAndReturnTabAt`; Close via `TabInterface::Close`. Build-only; binder registration lands in 6.3. |
| 0015 | `slayzone-Phase-6.2-inline_tab_webview_-slot-TabStripModel-wiring` | 6.2 | `SlayzoneRegionContainer` splits `mid`'s right pane vertically: `browserchrome` placeholder (40px URL-bar strip) over a native `views::WebView` (`inline_tab_webview_`) with no chrome:// slot of its own. `SlayzoneBrowserView` inherits `TabStripModelObserver`, syncs `SetWebContents(active)` at wire-up + on every non-selection-only change. Removes Phase 5.3 spike: `MaybeSpikeTerminalSlot`, `--slayzone-spike-url`, and the PostTask dance. Terminal slot is back to placeholder. |
| 0016 | `slayzone-Phase-6.3-C-binder-scaffold-Option-C-handof` | 6.3 | C++ binder only (Option C handoff). Two new WebUIControllers — `SlayzoneBrowserchromeUI` at `chrome://slayzone-browserchrome/` and `SlayzoneTabbarUI` at `chrome://slayzone-tabbar/` — both expose `BindInterface(slayzone::tabs::mojom::TabsHost)` that forwards to the owning `SlayzoneBrowserView`'s `SlayzoneTabsHost`. Back-pointer lookup via a `WebContentsUserData` installed on every region WebView's `WebContents` in `AddedToWidget` (`SlayzoneBrowserView::FromRegionWebContents`). Binder entries added to `chrome_browser_interface_binders_webui_parts_desktop.cc`. JS URL-bar + tab strip UIs are deferred to Phases 7.8 + 7.2 respectively; Phase 6 exit criteria are verified via CDP/programmatic drives in 6.4 + 6.5. |
| 0017 | `slayzone-Phase-7.1-statusbar-webui` | 7.1 | First real SlayZone surface in the fork. `chrome/browser/slayzone/mojom/statusbar.mojom` + `slayzone_statusbar_host.{h,cc}` — `StatusbarHost::GetSnapshot` forwards to the `SidecarClient` (`statusbar:get-snapshot` RPC). `chrome/browser/ui/webui/slayzone/slayzone_statusbar_ui.{h,cc}` — `chrome://slayzone-statusbar/` WebUIController that serves the `@slayzone/webui-statusbar` Vite bundle from `--slayzone-webui-bundle-dir/<region>/dist/` (fallback inline HTML when unset). `SlayzoneRegionContainer::RegionUrl` routes the `statusbar` region to the dedicated host. Binder + `ChromeURLHosts()` + WebUIConfig registrations follow the 0016 pattern. Outer repo: new `packages/webui/statusbar/` package, `packages/sidecar/src/handlers/statusbar.ts`, `scripts/chromium/run.sh` auto-launches the sidecar. |
| 0018–0050 | *(table rows owed)* | 7.2–16.4 | Exported patches exist on disk but were never added to this table (pre-existing drift). See each patch's commit message. |
| 0051 | `iteration-2-LayoutMode-fidelity-vs-Electron` | it-2 | `SlayzoneRegionContainer` gains `LayoutMode {kTaskDetail,kHome,kOverlay}` + `SetLayoutMode` (kHome = tasklist full-width, structural match w/ Electron). `LayoutHost.SetLayoutMode` mojo + `--slayzone-layout-mode=` CLI switch. Removes `home_panel_webview_`. |
| 0052 | `iteration-3-native-shell-visual-polish` | it-3 | `SlayzoneRegionContainer` surface-0 backdrop, 4-dip child spacing, 12-dip horizontal insets — Electron `bg-surface-0 + p-4 + gap-1` idiom at the native layout level. |
| 0053 | `iteration-3-LayoutHost-binder-for-tasklist` | it-3 | `SlayzoneTasklistUI` BindInterface(LayoutHost) so tasklist card clicks drive `SetLayoutMode(kTaskDetail)`. |
| 0054 | `cap-02-task-project-write-mojoms` | cap-02 | TasklistHost Create/Update/Delete/UpdateStatus/Move + ProjectsHost Create/Update/Delete, forwarding to sidecar SQLite handlers; shared `TaskWriteResult`/`ProjectWriteResult` envelopes. MoveTask wire-shape-only (sidecar deferred error until cap-11). |
| 0055 | `cap-03-tabs.mojom-TabType` | cap-03 | tabs.mojom adds `TabType` enum + `tab_type` field on `TabSnapshot`. |
| 0056 | `cap-03-TabsHost-TabType-classifier` | cap-03 | TabsHost URL→TabType classifier in `BuildSnapshot` (home/task-detail/overlay/aux). |
| 0057 | `cap-03-OverlayManager-search-dialogs` | cap-03 | OverlayManager adds `search` overlay id + **dialogs URL** + `active_overlay` getter. |
| 0058 | `cap-03-ShowOverlay-prior-mode` | cap-03 | `LayoutHost.ShowOverlay` saves prior LayoutMode, restores it on empty id. |
| 0059 | `cap-03-BrowserView-prior-mode-accelerators` | cap-03 | BrowserView `prior_mode_`, `OnTabStripModelChanged` auto-switch by TabType, Cmd+K/ESC accelerators. |
| 0060 | `cap-03-LayoutHost-binders` | cap-03 | LayoutHost binder on tabbar/tools/leaderboard/usage/context/dialogs WebUIs. |
| 0061 | `WebUIDataSource-MIME-HMR-Vite` | cap-shell | `GetMimeType` recognizes .tsx/.jsx/.mjs + routes `/@vite/`, `/@fs/`, `/@react-refresh` — enables chrome://slayzone-shell HMR via SimpleURLLoader proxy to the Vite dev server. |
| 0062 | `cap-shell-1-slayzone_hello-probe` | cap-shell-1 | Standalone smoke probe WebUI at `chrome://slayzone-hello/` for host-registration verification. |
| 0063 | `cap-shell-1-shell_ui-shell_container-registry` | cap-shell-1 | **The shell takeover.** `slayzone_shell_ui` (chrome://slayzone-shell host; dev-mode SimpleURLLoader proxy to Vite preserving chrome:// origin so Mojo works; CSP widened in dev) + `slayzone_shell_container` (replaces the 14-region grid with a full-bleed shell WebView + `embedded_tab_slot_`) + registry entry. |
| 0064 | `cap-shell-new-mojom-hosts` | cap-shell-2/4/5/6 | New hosts: `tags_host`; **`embedded_tab_host` (geometry-driven inline native browser — CreateView/SetBounds/Navigate/LRU)**; `windows_host` (aux windows via views::Widget) + `native_dialog_host`; `json_rpc_host` (generic long-tail escape hatch — new sidecar handlers without a chromium rebuild). |
| 0065 | `cap-shell-host-extensions-BUILD.gn` | cap-shell | Existing hosts (projects/tabs/terminal) extended; dialog/projects mojom fields; BUILD.gn + interface binders register all new hosts (shell/embedded_tab/json_rpc/tags/windows/native_dialog). |
| 0066 | `cap-shell-per-region-webui-hosts-refresh` | cap-shell | Per-region WebUI hosts refresh for the shell-era layout. |
| 0067 | `build-stub-eslint_ts-noop` | cap-layout-p4 | Stub the `eslint_ts()` gn template fork-wide to a no-op group. The fork tree lives inside the SlayZone pnpm monorepo; tsc-based webui lint steps resolve typescript/@typescript-eslint from the monorepo's node_modules via walk-up → type-instance mismatch + `ts_library.py` undeclared-definition assertions on clean builds. Lint-only, no product impact. Restore once `src/node_modules` is pinned to the vendored toolchain. |
| 0068 | `native-dialog-overlay-above-live-tab` | cap-layout-p4 | `SlayzoneOverlayManager` `"dialog"` overlay id → `chrome://slayzone-shell/#overlay=dialog` with a TRANSPARENT page base background (live embedded tab visible underneath). `SlayzoneShellUI` LayoutHost `BindInterface` + binder so the shell page drives `ShowOverlay`. Fixes `HandleShellRequest` to strip `?`/`#` from the data-source path before file resolution (the chrome:// router leaks query/fragment into `path` on this build → empty body). |
| 0069 | `extension-capable-inline-tabs` | ext-option-b-s1 | Embedded browser panes back onto real tabs in a windowless per-task `Browser` (was bare `WebContents::Create`) → tabs are `chrome.tabs`-addressable + get `TabHelpers` (foundation for extensions + per-task profiles); each task = its own `windowId`, the Browser never `Show()`s an OS window. Marker `user_title=kEmbeddedTabBrowserMarker` (+`omit_from_session_restore`) opts helpers out of the `SlayzoneBrowserView` shell takeover in `browser_window_factory` (else each loads `chrome://slayzone-shell`, opens a pane, recurses ∞). Four display fixes: `IncrementCapturerCount` keepalive (a tab in a never-shown Browser is marked hidden → no frames); a dummy active `about:blank` tab so content tabs stay BACKGROUND (a BrowserView owns its active tab's RWHV); NON-idempotent CreateView (the renderer's `NativeAnchor` remounts → a shared view_id let the old anchor's teardown destroy the live tab); re-attach the slot on `PrimaryPageChanged` (cross-origin process swap → new RWHV → URL updates but page froze otherwise). |
| 0070 | `windowless-panes-extension-mgmt-window` | ext-option-b-s1 | `SlayzoneHiddenBrowserView` (subclass of the real `BrowserView`, no-op `Show`/`Activate`/`Restore`/etc.) → the embedded-tab Browsers hold extension-capable tabs but NEVER surface an OS window (install / `window.open` / `target=_blank` no longer pop a closable window that kills the pane). Plus a single VISIBLE extension-management window (`kExtensionsBrowserMarker` → plain `BrowserView`, 80% centered, opened to the Web Store via the `slayzone:open-extensions` CreateView sentinel) on the shared profile — the install flow needs a TABBED visible window to anchor its prompt (`extension_install_ui_desktop.cc:FindOrCreateVisibleBrowser`), which a hidden pane can't give, so management gets its own real window; installs land in the shared profile and apply to the panes. Themed dark + SlayZone accent `#7c7cf0` via `ThemeService`. |
| 0076 | `in-pane-new-window-adoption` | panes | In-pane `window.open` / `target="_blank"` / Cmd+click → a new pane **tab** (was a lost background tab). The host observes each task Browser's `TabStripModel`; a tab it didn't create (guarded by `creating_owned_tab_` around our own `AddAndReturnTabAt`) is **adopted** — minted a `view_id` + `Entry` — and surfaced via a new `EmbeddedTabObserver.OnNewView(task_id, view_id, url)`. The renderer opens a browser tab bound to that existing view via a new `AttachView(view_id, bounds)` (composites without minting/loading a fresh tab). `TabStripModelObserver` base auto-cleans on model destroy. Renderer side (monorepo): `@slayzone/layout` renders ALL tiles in a pane (inactive `display:none`) so a tab's native view persists across switches, and `NativeAnchor` composites only when its tile is the active tab (gated `active`); `embedded-tab-host` DEFERS destroy of an adopted view so it survives the NativeAnchor layout-settle remount (the page's real tab is unrecreatable). **NEW_TAB only** — sized `window.open` popups (NEW_POPUP / OAuth) are a follow-on. |
| 0075 | `hide-switch-to-chrome-nag` | ext-modal | Hide the Web Store's "Switch to Chrome?" promo in the extensions inlay. That nag is served only to non-Chrome browsers via Google's proprietary `X-Client-Data` signal (which a Chromium fork can't legitimately satisfy — the UA brand alone doesn't suppress it). A `NagHider` `WebContentsObserver` on the inlay's Web Store tab injects, at navigation **commit** (document-start → no flash), an isolated-world script (`ExecuteJavaScriptInIsolatedWorld`, `ISOLATED_WORLD_ID_CHROME_INTERNAL`) that hides the promo by **text** match ("recommends using chrome"/"switch to chrome"), pierces shadow roots, and re-hides via a throttled `MutationObserver`. Selector-free → durable; refresh only if Google changes the wording. **Pairs with the monorepo `run.sh` `--hide-crash-restore-bubble` flag** that suppresses the "Restore pages?" bubble. |
| 0074 | `extension-bar-live-relist` | ext-modal | The bar (0073) listed extensions once on open → a freshly-installed extension didn't show until reopen. Host now implements `extensions::ExtensionRegistryObserver` (via `ScopedMultiSourceObservation`); `ListExtensions` starts observing that identity's registry; `OnExtensionLoaded`/`OnExtensionUnloaded` → fire a new `EmbeddedTabObserver.OnExtensionsChanged()` (no payload) → the renderer re-lists live. `OnShutdown` drops the observation. |
| 0073 | `extension-bar` | ext-modal | The chromeless inlay (0072) removed Chrome's extension toolbar, leaving installed extensions (1Password) with **no entry point**. Adds a minimal one — a **rail** in the extensions modal listing the identity's extensions and opening each one's UI. mojom: `ListExtensions(profile_key)` → `array<ExtensionInfo{id,name,enabled,has_options,has_popup}>`, `OpenExtensionOptions(id)`, `OpenExtensionPopup(id)`. Host enumerates `ExtensionRegistry::Get(profile)->enabled_extensions()` (skip component + non-`is_extension`); `has_options` via `OptionsPageInfo`, `has_popup` via `ExtensionActionManager`/`ExtensionAction`. Options → `ExtensionTabUtil::OpenOptionsPage`; popup → navigate the inlay's active tab to the action's popup URL (the extension's own UI, e.g. the 1Password vault — loaded as a page, not an anchored bubble). `slayzone_shell_ui` widens the `img-src` CSP with `chrome://extension-icon` so the rail shows real icons. Per-identity. |
| 0072 | `extensions-modal-chromeless-inlay` | ext-modal | The detached extension-management window becomes a real SlayZone **modal**: the React shell draws the scrim/card/header, and the Web Store renders in a chromeless, frameless, rounded child browser **"inlay"** pinned over the card's body rect. `SlayzoneInlayBrowserView` hides the chrome — the tab strip region is a DIRECT child of `BrowserView` (added last to paint on top), a sibling of `top_container` not inside it, so it's hidden on its own alongside `top_container` (toolbar/bookmark); `contents_container` fills the window; the Web Store stays the normal ACTIVE tab so install prompts/autofill anchor as in stock Chrome. `BrowserNativeWidgetMac` (gated on `kExtensionsBrowserMarker`): `kFrameless` (no traffic lights, mirrors document-PiP), rounded corners (12) + translucent, no window animation, non-movable; `can_resize=false` via `CreateParams`. Host opens the inlay as a CHILD of the shell window (`Widget::Reparent`) pinned to the React body rect (`BodyToScreen`: CSS px → screen DIPs via container origin); `SetExtensionsBounds` tracks move/resize, `CloseExtensions` tears it down, Store\|Manage navigates the active tab. |
| 0071 | `per-task-pooled-profiles` | ext-option-b-s2 | Per-task identities: `CreateViewParams` gains `profile_key`. `ResolveProfile` maps it to a `Profile` — empty = the shell's default (sync); a pooled key → get-or-create the `slayzone-<key>` profile (`ProfileManager::CreateProfileAsync` on first use) + a `ScopedProfileKeepAlive` (origin `kHeadlessCommand`). `CreateView` splits into resolve → `CreateViewOnProfile`. `GetOrCreateTaskBrowser(task_id, profile)` rebuilds the task's windowless Browser on the new profile when the identity changes. `OpenExtensionsWindow(profile_key)` opens the management window on the chosen identity → Web-Store installs (1Password) land in that profile and apply to its panes. Per-profile cookie + extension isolation is architectural (`StoragePartition` / `ExtensionRegistry`); verified two panes on `work`/`personal` get independent cookie jars. |

Patches not yet authored against real source are tracked in `files/<name>/`
— raw assets + a README describing the context-sensitive edits still owed.

## Authoring

```sh
# in chromium/src/ after source fetch
git checkout -b slayzone/<patch-name>
# ...make edits...
git add -A && git commit
git format-patch -1 --output-directory ../../patches/chromium/ \
  --start-number <N> --subject-prefix=""
```

Patches are rebased onto each Chromium version bump by the merge bot (C1,
activated in Phase 2).
